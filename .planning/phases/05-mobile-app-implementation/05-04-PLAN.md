---
phase: "05"
plan: "04"
type: "execute"
wave: 1
depends_on: ["05-03"]
files_modified: [
  "code/apps/mobile/app/ride/create.tsx",
  "code/apps/mobile/app/ride/manage.tsx",
  "code/apps/mobile/app/booking/[id].tsx",
  "code/apps/mobile/src/components/LocationPicker.tsx",
  "code/apps/mobile/src/services/ride.service.ts",
  "code/apps/mobile/src/services/booking.service.ts"
]
autonomous: true
requirements: ["MOB-05"]

must_haves:
  truths:
    - "Tài xế có thể đăng chuyến đi mới từ ứng dụng mobile"
    - "Component chọn địa điểm hoạt động với autocomplete"
    - "Tài xế có thể xem danh sách các yêu cầu đặt chỗ"
    - "Chức năng Phê duyệt/Từ chối đặt chỗ hoạt động trên mobile"
  artifacts:
    - path: "code/apps/mobile/app/ride/create.tsx"
      provides: "Giao diện và logic Đăng chuyến đi"
    - path: "code/apps/mobile/src/components/LocationPicker.tsx"
      provides: "Component chọn địa điểm với Google Places Autocomplete"
    - path: "code/apps/mobile/app/booking/[id].tsx"
      provides: "Màn hình chi tiết booking và quản lý trạng thái"
  key_links:
    - from: "apps/mobile"
      to: "google-places-autocomplete"
      via: "Location Picker"
---

<objective>
Triển khai các tính năng dành cho Tài xế bao gồm đăng chuyến đi và quản lý yêu cầu từ hành khách trên ứng dụng di động.
Mục tiêu:
1. Xây dựng form Đăng chuyến đi với tính năng chọn địa điểm trực quan.
2. Triển khai màn hình Quản lý chuyến đi do người dùng tạo.
3. Xây dựng giao diện phê duyệt/từ chối các yêu cầu đặt chỗ từ hành khách.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng màn hình Đăng chuyến đi (Create Ride)</name>
  <files>code/apps/mobile/app/ride/create.tsx, code/apps/mobile/src/components/LocationPicker.tsx</files>
  <action>
    - Triển khai form sử dụng `react-hook-form` và schema từ shared.
    - Xây dựng `LocationPicker` tích hợp Google Places Autocomplete cho RN.
    - Xử lý các trường thông tin: Điểm đi/đến, Thời gian khởi hành, Giá tiền, Số chỗ.
    - Gọi API `/api/rides` để tạo chuyến đi mới.
  </action>
  <verify>
    <manual>Thực hiện đăng chuyến đi từ mobile và xác nhận chuyến đi xuất hiện trên ứng dụng Web.</manual>
  </verify>
  <done>Tài xế có thể đăng bài tìm hành khách trực tiếp từ điện thoại.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Quản lý chuyến đi & Yêu cầu</name>
  <files>code/apps/mobile/app/ride/manage.tsx, code/apps/mobile/app/booking/[id].tsx</files>
  <action>
    - Xây dựng màn hình danh sách chuyến đi do user hiện tại đăng.
    - Hiển thị số lượng yêu cầu đặt chỗ (booking request) của mỗi chuyến đi.
    - Tạo màn hình chi tiết yêu cầu `booking/[id].tsx` hiển thị thông tin hành khách (Avatar, Bio, Rating).
  </action>
  <verify>
    <manual>Kiểm tra việc hiển thị đúng danh sách các booking chờ duyệt.</manual>
  </verify>
  <done>Tài xế có cái nhìn tổng quan về tình trạng các chuyến đi mình đăng.</done>
</task>

<task type="auto">
  <name>Task 3: Triển khai logic Phê duyệt/Từ chối Booking</name>
  <files>code/apps/mobile/app/booking/[id].tsx, code/apps/mobile/src/services/booking.service.ts</files>
  <action>
    - Thêm các nút điều khiển (Approve/Reject) vào màn hình booking.
    - Gọi API `PATCH /api/bookings/:id/status` để cập nhật trạng thái.
    - Xử lý các feedback (thành công/lỗi) cho người dùng qua thông báo UI.
    - Làm mới dữ liệu (Refetch) sau khi cập nhật thành công.
  </action>
  <verify>
    <manual>Phê duyệt 1 yêu cầu trên mobile và kiểm tra trạng thái cập nhật trên Web.</manual>
  </verify>
  <done>Tài xế có toàn quyền quản lý hành khách đi cùng thông qua app mobile.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Broken Access Control | API endpoints | mitigate | Backend cần kiểm tra `driverId` có khớp với `userId` hiện tại khi update booking status. |
</threat_model>

<success_criteria>
- Đăng chuyến đi thành công và dữ liệu tọa độ được lưu trữ chính xác.
- Danh sách booking requests hiển thị đầy đủ thông tin hành khách.
- Logic phê duyệt/từ chối hoạt động ổn định và phản hồi tức thì.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-mobile-app-implementation/05-04-SUMMARY.md`
</output>
