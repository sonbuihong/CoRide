---
phase: "05"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: [
  "code/apps/mobile/package.json",
  "code/apps/mobile/app/_layout.tsx",
  "code/apps/mobile/app/(tabs)/_layout.tsx",
  "code/apps/mobile/app/(tabs)/index.tsx",
  "code/apps/mobile/tailwind.config.js",
  "code/apps/mobile/global.css",
  "code/package.json"
]
autonomous: true
requirements: ["MOB-01", "MOB-02"]

must_haves:
  truths:
    - "Dự án Expo được khởi tạo thành công trong apps/mobile"
    - "Cấu trúc Expo Router (tabs) được thiết lập"
    - "Nativewind v4 được cấu hình hoàn chỉnh với Tailwind CSS"
    - "Ứng dụng mobile có thể import các package shared từ monorepo"
  artifacts:
    - path: "code/apps/mobile/package.json"
      provides: "Dependencies cho Expo & React Native"
    - path: "code/apps/mobile/tailwind.config.js"
      provides: "Cấu hình Nativewind cho mobile"
    - path: "code/apps/mobile/app/_layout.tsx"
      provides: "Root layout với Providers (Auth, QueryClient)"
  key_links:
    - from: "apps/mobile"
      to: "packages/shared"
      via: "npm workspace dependency"
---

<objective>
Thiết lập nền tảng dự án Mobile bằng Expo và tích hợp vào Monorepo CoRide.
Mục tiêu:
1. Khởi tạo ứng dụng Expo với TypeScript và Expo Router.
2. Cấu hình Nativewind v4 để đồng bộ phong cách styling với Web.
3. Thiết lập kết nối với các package shared (schemas, types).
4. Xây dựng khung giao diện chính (Tab Navigation).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Khởi tạo dự án Expo trong Monorepo</name>
  <files>code/apps/mobile/package.json, code/package.json</files>
  <action>
    - Sử dụng `npx create-expo-app@latest apps/mobile --template tabs`.
    - Cập nhật name trong `apps/mobile/package.json` thành `@repo/mobile`.
    - Thêm `@repo/shared` vào dependencies của apps/mobile.
    - Chạy `npm install` tại root để link các workspace.
    - Đảm bảo script build/dev/lint trong apps/mobile tương thích với Turbo.
  </action>
  <verify>
    <automated>npx turbo run build --filter=@repo/mobile</automated>
  </verify>
  <done>Dự án Expo đã sẵn sàng và được tích hợp vào hệ thống build của Monorepo.</done>
</task>

<task type="auto">
  <name>Task 2: Cấu hình Nativewind v4 (Tailwind cho Mobile)</name>
  <files>code/apps/mobile/tailwind.config.js, code/apps/mobile/global.css, code/apps/mobile/app/_layout.tsx</files>
  <action>
    - Cài đặt `nativewind@latest` và `tailwindcss`.
    - Tạo `tailwind.config.js` cấu hình content trỏ tới các file trong app/ và src/components/.
    - Tạo `global.css` với các chỉ thị tailwind.
    - Import `global.css` vào root `_layout.tsx`.
  </action>
  <verify>
    <manual>Kiểm tra styles được áp dụng trên HomeScreen.</manual>
  </verify>
  <done>Nativewind đã được thiết lập, cho phép dùng Tailwind classes trên React Native components.</done>
</task>

<task type="auto">
  <name>Task 3: Thiết lập cấu trúc Navigation (Expo Router)</name>
  <files>code/apps/mobile/app/_layout.tsx, code/apps/mobile/app/(tabs)/_layout.tsx, code/apps/mobile/app/(tabs)/index.tsx</files>
  <action>
    - Định nghĩa Root Layout với `Stack` navigation.
    - Thiết lập Tab Layout `(tabs)/_layout.tsx` bao gồm các tabs: Trang chủ (Home), Chuyến đi của tôi (My Rides), Thông báo (Notifications), Cá nhân (Profile).
    - Sử dụng Lucide React Native cho các tab icons.
  </action>
  <verify>
    <automated>npx expo lint</automated>
  </verify>
  <done>Khung điều hướng Tab và Stack đã được thiết lập chuẩn xác.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Local Storage | mitigate | Không lưu thông tin nhạy cảm vào AsyncStorage, sử dụng SecureStore ở các phase sau. |
</threat_model>

<success_criteria>
- Ứng dụng mobile khởi tạo thành công với Expo Router.
- Nativewind v4 hoạt động (styles được áp dụng).
- Có thể import và sử dụng Zod schema từ `@repo/shared`.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-mobile-app-implementation/05-01-SUMMARY.md`
</output>
