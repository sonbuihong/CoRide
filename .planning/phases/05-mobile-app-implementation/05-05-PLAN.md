---
phase: "05"
plan: "05"
type: "execute"
wave: 1
depends_on: ["05-04"]
files_modified: [
  "code/apps/mobile/app/(tabs)/notifications.tsx",
  "code/apps/mobile/src/hooks/useNotifications.ts",
  "code/apps/mobile/src/hooks/useSSE.ts",
  "code/apps/mobile/src/services/notification.service.ts",
  "code/apps/mobile/app/_layout.tsx"
]
autonomous: true
requirements: ["MOB-06", "MOB-07"]

must_haves:
  truths:
    - "Ứng dụng lấy được Expo Push Token và gửi về Backend thành công"
    - "Kết nối SSE hoạt động trên mobile và tự động reconnect"
    - "Màn hình Notification Inbox hiển thị danh sách thông báo chính xác"
    - "Thông báo đẩy xuất hiện trên thiết bị khi nhận được data từ Backend"
  artifacts:
    - path: "code/apps/mobile/src/hooks/useNotifications.ts"
      provides: "Hook quản lý Expo Notifications (Push)"
    - path: "code/apps/mobile/src/hooks/useSSE.ts"
      provides: "Hook quản lý Server-Sent Events trên Mobile"
    - path: "code/apps/mobile/app/(tabs)/notifications.tsx"
      provides: "Giao diện hộp thư thông báo"
  key_links:
    - from: "apps/mobile"
      to: "expo-notifications"
      via: "Push service"
---

<objective>
Tích hợp hệ thống thông báo thời gian thực và thông báo đẩy vào ứng dụng di động CoRide.
Mục tiêu:
1. Thiết lập Expo Notifications để nhận Push Tokens và hiển thị thông báo.
2. Xây dựng hook `useSSE` để đồng bộ dữ liệu thông báo tức thì khi ứng dụng đang mở.
3. Triển khai màn hình Danh sách thông báo (Notification Inbox).
4. Đồng bộ trạng thái đọc (read/unread) thông báo với Backend.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Cấu hình Expo Push Notifications</name>
  <files>code/apps/mobile/src/hooks/useNotifications.ts, code/apps/mobile/app/_layout.tsx</files>
  <action>
    - Cài đặt `expo-notifications` và `expo-device`.
    - Viết logic lấy `expoPushToken` và xin quyền thông báo từ người dùng.
    - Gọi API để gửi token về Backend lưu trữ tại bảng `User`.
    - Thiết lập notification handler để xử lý thông báo khi app đang ở background/foreground.
  </action>
  <verify>
    <manual>Kiểm tra token xuất hiện trong DB sau khi grant permission trên thiết bị.</manual>
  </verify>
  <done>Ứng dụng đã sẵn sàng nhận thông báo đẩy từ máy chủ.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Real-time SSE Sync</name>
  <files>code/apps/mobile/src/hooks/useSSE.ts, code/apps/mobile/src/services/notification.service.ts</files>
  <action>
    - Sử dụng `react-native-eventsource` để kết nối tới endpoint `/api/notifications/stream`.
    - Lắng nghe các sự kiện: `BOOKING_REQUEST`, `BOOKING_STATUS_UPDATE`.
    - Tự động làm mới danh sách thông báo hoặc dữ liệu chuyến đi khi nhận được event.
    - Xử lý logic tự động kết nối lại khi mất mạng.
  </action>
  <verify>
    <manual>Thực hiện một hành động (ví dụ đặt chỗ) trên Web và kiểm tra Mobile có cập nhật thông báo ngay lập tức không.</manual>
  </verify>
  <done>Ứng dụng mobile đã được đồng bộ dữ liệu thời gian thực với hệ thống.</done>
</task>

<task type="auto">
  <name>Task 3: Xây dựng giao diện Notification Inbox</name>
  <files>code/apps/mobile/app/(tabs)/notifications.tsx</files>
  <action>
    - Xây dựng màn hình danh sách thông báo sử dụng `FlatList`.
    - Phân loại icon theo từng loại thông báo (Booking, Status, Review).
    - Implement chức năng "Đánh dấu đã đọc" (Mark as read).
    - Hiển thị badge số lượng thông báo chưa đọc tại Tab bar icon.
  </action>
  <verify>
    <manual>Kiểm tra danh sách thông báo hiển thị đúng các thông báo cũ và mới.</manual>
  </verify>
  <done>Người dùng có thể quản lý lịch sử thông báo của mình trên điện thoại.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Push Payloads | mitigate | Không gửi dữ liệu nhạy cảm trong payload của push notification. Chỉ gửi ID và tiêu đề ngắn gọn. |
</threat_model>

<success_criteria>
- Push Token được đăng ký thành công cho mỗi user.
- SSE kết nối ổn định và nhận được các event thời gian thực.
- Badge thông báo hiển thị chính xác số lượng unread.
- Ứng dụng hiển thị thông báo hệ thống (system notification) khi ở chế độ nền.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-mobile-app-implementation/05-05-SUMMARY.md`
</output>
