---
phase: "05"
plan: "03"
type: "execute"
wave: 1
depends_on: ["05-02"]
files_modified: [
  "code/apps/mobile/app/(tabs)/index.tsx",
  "code/apps/mobile/app/ride/[id].tsx",
  "code/apps/mobile/src/services/ride.service.ts",
  "code/apps/mobile/src/components/RideCard.tsx",
  "code/apps/mobile/src/components/RideMap.tsx"
]
autonomous: true
requirements: ["MOB-04"]

must_haves:
  truths:
    - "Danh sách chuyến đi được hiển thị từ API Backend"
    - "Người dùng có thể tìm kiếm chuyến đi"
    - "Bản đồ hiển thị lộ trình khi xem chi tiết chuyến đi"
    - "Chức năng đặt chỗ (Booking) hoạt động trên mobile"
  artifacts:
    - path: "code/apps/mobile/src/components/RideCard.tsx"
      provides: "Thẻ hiển thị thông tin chuyến đi"
    - path: "code/apps/mobile/src/components/RideMap.tsx"
      provides: "Component hiển thị bản đồ với react-native-maps"
    - path: "code/apps/mobile/app/ride/[id].tsx"
      provides: "Trang chi tiết chuyến đi"
  key_links:
    - from: "apps/mobile"
      to: "react-native-maps"
      via: "Map implementation"
---

<objective>
Triển khai các tính năng tìm kiếm, xem chi tiết và đặt chuyến đi dành cho Hành khách trên ứng dụng di động.
Mục tiêu:
1. Xây dựng trang chủ hiển thị danh sách chuyến đi và bộ lọc tìm kiếm.
2. Thiết kế và triển khai thẻ chuyến đi (Ride Card) trực quan.
3. Tích hợp bản đồ vào trang chi tiết chuyến đi để hiển thị lộ trình.
4. Xử lý logic đặt chỗ và gửi yêu cầu tới Backend.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Triển khai Danh sách chuyến đi & Tìm kiếm</name>
  <files>code/apps/mobile/app/(tabs)/index.tsx, code/apps/mobile/src/services/ride.service.ts, code/apps/mobile/src/components/RideCard.tsx</files>
  <action>
    - Sử dụng `TanStack Query` để fetch danh sách chuyến đi từ `/api/rides`.
    - Triển khai `RideCard` với đầy đủ thông tin: Điểm đi/đến, giá, số chỗ, thời gian.
    - Xây dựng thanh tìm kiếm đơn giản (Search bar) tại trang Home.
    - Implement pull-to-refresh để cập nhật dữ liệu.
  </action>
  <verify>
    <manual>Kiểm tra dữ liệu chuyến đi từ Backend hiển thị đúng trên danh sách Mobile.</manual>
  </verify>
  <done>Màn hình chính đã hiển thị đầy đủ thông tin các chuyến đi đang khả dụng.</done>
</task>

<task type="auto">
  <name>Task 2: Tích hợp Bản đồ & Chi tiết chuyến đi</name>
  <files>code/apps/mobile/app/ride/[id].tsx, code/apps/mobile/src/components/RideMap.tsx</files>
  <action>
    - Tạo `RideMap` component sử dụng `react-native-maps`.
    - Vẽ lộ trình (Polyline) đơn giản hoặc Markers cho điểm bắt đầu và kết thúc.
    - Hiển thị thông tin chi tiết về tài xế (Avatar, Bio, Rating).
    - Thiết lập Expo Router `ride/[id]` để nhận ID từ params.
  </action>
  <verify>
    <manual>Kiểm tra bản đồ có hiển thị đúng tọa độ của chuyến đi khi mở chi tiết.</manual>
  </verify>
  <done>Người dùng có thể xem lộ trình trực quan trên bản đồ trước khi đặt chỗ.</done>
</task>

<task type="auto">
  <name>Task 3: Triển khai luồng Đặt chỗ (Booking)</name>
  <files>code/apps/mobile/app/ride/[id].tsx, code/apps/mobile/src/services/booking.service.ts</files>
  <action>
    - Thêm nút "Đặt chỗ ngay" vào màn hình chi tiết.
    - Tạo Modal xác nhận số lượng ghế muốn đặt.
    - Gọi API `/api/bookings` để gửi yêu cầu.
    - Hiển thị thông báo thành công và redirect về tab "Chuyến đi của tôi".
  </action>
  <verify>
    <manual>Thực hiện đặt chỗ trên app mobile và kiểm tra bản ghi tương ứng trong DB backend.</manual>
  </verify>
  <done>Hành khách có thể gửi yêu cầu đặt chỗ thành công thông qua ứng dụng di động.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Map API | mitigate | Không để lộ API Key trong mã nguồn client-side, sử dụng các biến môi trường và giới hạn API key theo Bundle ID. |
</threat_model>

<success_criteria>
- Danh sách chuyến đi hiển thị mượt mà với pull-to-refresh.
- Tìm kiếm trả về kết quả chính xác theo bộ lọc.
- Bản đồ hiển thị tại màn hình chi tiết chuyến đi.
- Luồng đặt chỗ hoạt động và dữ liệu được đồng bộ về Backend.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-mobile-app-implementation/05-03-SUMMARY.md`
</output>
