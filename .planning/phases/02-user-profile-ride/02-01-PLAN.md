---
phase: 02-user-profile-ride
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/packages/database/prisma/schema.prisma
  - code/packages/shared/src/profile.schema.ts
  - code/packages/shared/src/ride.schema.ts
  - code/packages/shared/src/index.ts
  - code/apps/backend/src/config/cloudinary.ts
autonomous: true
requirements: [PROF-01, UPLD-01, RIDE-01, SRCH-01]
user_setup:
  - service: cloudinary
    why: "Lưu trữ ảnh đại diện người dùng"
    env_vars:
      - name: CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Dashboard"
      - name: CLOUDINARY_API_KEY
        source: "Cloudinary Dashboard"
      - name: CLOUDINARY_API_SECRET
        source: "Cloudinary Dashboard"

must_haves:
  truths:
    - "Model User trong database có trường bio"
    - "Shared schemas cho Profile và Ride đã sẵn sàng"
    - "Cloudinary config đã được thiết lập"
  artifacts:
    - path: "code/packages/database/prisma/schema.prisma"
      provides: "User bio field and Ride model (if missing)"
    - path: "code/packages/shared/src/profile.schema.ts"
      provides: "Zod validation cho profile update"
    - path: "code/packages/shared/src/ride.schema.ts"
      provides: "Zod validation cho ride posting and search"
    - path: "code/apps/backend/src/config/cloudinary.ts"
      provides: "Cấu hình kết nối Cloudinary"
  key_links:
    - from: "code/packages/shared/src/index.ts"
      to: "code/packages/shared/src/profile.schema.ts"
      via: "export *"
---

<objective>
Thiết lập nền tảng dữ liệu và cấu hình cho Giai đoạn 2. Bao cập cập nhật Prisma schema, tạo các Zod schema dùng chung (Shared Schemas) và cấu hình Cloudinary cho việc tải ảnh.

Mục tiêu: Đảm bảo các hợp đồng dữ liệu (contracts) và hạ tầng lưu trữ sẵn sàng cho việc triển khai API và UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@code/packages/database/prisma/schema.prisma
@code/packages/shared/src/auth.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật Database Schema & Migrations</name>
  <files>code/packages/database/prisma/schema.prisma</files>
  <action>
    - Thêm trường `bio String?` vào model `User`.
    - Kiểm tra model `Ride` đã đầy đủ các trường: `origin`, `destination`, `departureTime`, `availableSeats`, `pricePerSeat`, `status`, `description`.
    - Chạy lệnh migration: `npx prisma migrate dev --name add_user_bio --schema=code/packages/database/prisma/schema.prisma`
    - Generate client: `npx prisma generate --schema=code/packages/database/prisma/schema.prisma`
  </action>
  <verify>
    <automated>npx prisma validate --schema=code/packages/database/prisma/schema.prisma</automated>
  </verify>
  <done>Model User có trường bio, migration đã chạy thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo Shared Zod Schemas cho Profile & Ride</name>
  <files>code/packages/shared/src/profile.schema.ts, code/packages/shared/src/ride.schema.ts, code/packages/shared/src/index.ts</files>
  <action>
    - Tạo `profile.schema.ts`: `updateProfileSchema` (firstName, lastName, phone, bio). Sử dụng Regex cho SĐT Việt Nam (tương tự `auth.schema.ts`).
    - Tạo `ride.schema.ts`: 
      - `createRideSchema`: origin, destination, departureTime (ISO string, future date), availableSeats (int, min 1), pricePerSeat (number, min 0), description (optional).
      - `searchRideSchema`: origin (optional), destination (optional), date (optional).
    - Export tất cả từ `index.ts`.
  </action>
  <verify>
    <automated>cd code/packages/shared && npm run build</automated>
  </verify>
  <done>Shared schemas được tạo và export thành công.</done>
</task>

<task type="auto">
  <name>Task 3: Cấu hình Cloudinary & Multer trong Backend</name>
  <files>code/apps/backend/src/config/cloudinary.ts, code/apps/backend/package.json</files>
  <action>
    - Cài đặt dependencies: `npm install multer cloudinary multer-storage-cloudinary` trong `apps/backend`.
    - Tạo file `config/cloudinary.ts` sử dụng `cloudinary` và `multer-storage-cloudinary`.
    - Thiết lập storage cho `coride/avatars`, giới hạn định dạng: `jpg, png, jpeg`, kích thước: 500x500 crop.
    - Export `upload` middleware instance.
  </action>
  <verify>
    <automated>grep -q "cloudinary" code/apps/backend/package.json && ls code/apps/backend/src/config/cloudinary.ts</automated>
  </verify>
  <done>Cloudinary đã được cấu hình và middleware upload đã sẵn sàng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Client (Web/Mobile) → Backend API (File upload & Schema validation)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Profile Update | mitigate | Validate inputs using Zod (shared schemas) at API entry. |
| T-02-01-02 | Tampering | Avatar Upload | mitigate | Limit file types and size in Multer config. Cloudinary handles image sanitization. |
</threat_model>

<verification>
Kiểm tra prisma schema hợp lệ, shared schemas compile được, và backend cài đặt đủ thư viện.
</verification>

<success_criteria>
1. Prisma model User có bio.
2. `@repo/shared` export được Profile và Ride schemas.
3. Backend có file `config/cloudinary.ts` hợp lệ.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-ride/02-01-SUMMARY.md`
</output>
