---
phase: 02-user-profile-ride
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - code/apps/web/src/app/profile/page.tsx
  - code/apps/web/src/components/profile/profile-form.tsx
  - code/apps/web/src/components/profile/avatar-upload.tsx
autonomous: true
requirements: [PROF-UI]

must_haves:
  truths:
    - "Trang Hồ sơ hiển thị đầy đủ thông tin hiện tại"
    - "User có thể cập nhật thông tin và upload avatar qua giao diện"
    - "Giao diện sử dụng Tiếng Việt 100%"
  artifacts:
    - path: "code/apps/web/src/app/profile/page.tsx"
      provides: "Giao diện trang cá nhân"
    - path: "code/apps/web/src/components/profile/profile-form.tsx"
      provides: "Form cập nhật thông tin Tiếng Việt"
---

<objective>
Xây dựng giao diện Web cho việc quản lý hồ sơ cá nhân. Đảm bảo trải nghiệm người dùng mượt mà, hỗ trợ tải ảnh và thông báo phản hồi bằng Tiếng Việt.

Output: Trang `/profile` hoàn thiện với đầy đủ chức năng.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@code/apps/web/src/components/auth/auth-form.tsx
@code/apps/web/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng Profile Form & Avatar Upload Components</name>
  <files>code/apps/web/src/components/profile/profile-form.tsx, code/apps/web/src/components/profile/avatar-upload.tsx</files>
  <action>
    - Tạo `profile-form.tsx` sử dụng `react-hook-form` và `updateProfileSchema` từ `@repo/shared`.
    - Tạo `avatar-upload.tsx` để chọn file và preview ảnh trước khi upload.
    - Toàn bộ Label, Placeholder và Button bằng Tiếng Việt.
    - Sử dụng `shadcn/ui` (Input, Button, Card, Avatar) để giữ style đồng bộ.
  </action>
  <verify>
    <automated>ls code/apps/web/src/components/profile/profile-form.tsx</automated>
  </verify>
  <done>Các components UI cho profile đã được tạo.</done>
</task>

<task type="auto">
  <name>Task 2: Hoàn thiện Trang Profile & Kết nối API</name>
  <files>code/apps/web/src/app/profile/page.tsx</files>
  <action>
    - Tạo trang `/profile` (Client Component).
    - Fetch thông tin user hiện tại (Sử dụng `getMe` từ Auth flow cũ).
    - Handle Submit Form: Gọi `PATCH /api/users/profile`.
    - Handle Avatar Upload: Gọi `POST /api/users/profile/avatar`.
    - Sử dụng `sonner` hoặc `toast` để hiển thị thông báo thành công/lỗi bằng Tiếng Việt.
  </action>
  <verify>
    <automated>grep -q "api/users/profile" code/apps/web/src/app/profile/page.tsx</automated>
  </verify>
  <done>Trang Profile hoạt động hoàn chỉnh với Backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser → Backend API (JWT authentication)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Profile Form | mitigate | Use Zod validation on client-side before sending requests. |
| T-02-04-02 | Information Disclosure | Auth Token | mitigate | Ensure token is handled securely (httpOnly cookies as per Phase 1). |
</threat_model>

<verification>
Truy cập `/profile`, thử cập nhật thông tin và upload ảnh.
</verification>

<success_criteria>
1. Form profile hiển thị đúng dữ liệu người dùng.
2. Cập nhật thông tin thành công và có thông báo toast Tiếng Việt.
3. Avatar cập nhật ngay lập tức sau khi upload.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-ride/02-04-SUMMARY.md`
</output>
