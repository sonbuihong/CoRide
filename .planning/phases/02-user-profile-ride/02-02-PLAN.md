---
phase: 02-user-profile-ride
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - code/apps/backend/src/controllers/user.controller.ts
  - code/apps/backend/src/routes/user.routes.ts
  - code/apps/backend/src/services/user.service.ts
  - code/apps/backend/src/tests/profile.test.ts
autonomous: true
requirements: [PROF-01, UPLD-01]

must_haves:
  truths:
    - "User có thể cập nhật profile thành công qua API"
    - "User có thể upload avatar thành công qua API"
    - "Thông báo lỗi/thành công bằng Tiếng Việt 100%"
  artifacts:
    - path: "code/apps/backend/src/tests/profile.test.ts"
      provides: "Unit & Integration tests cho profile"
    - path: "code/apps/backend/src/services/user.service.ts"
      provides: "Logic nghiệp vụ xử lý profile"
---

<objective>
Triển khai API cập nhật hồ sơ người dùng và tải ảnh đại diện bằng phương pháp TDD. Đảm bảo logic nghiệp vụ được kiểm thử 100% và thông báo trả về bằng Tiếng Việt.

Output: API `PATCH /api/users/profile` và `POST /api/users/profile/avatar` hoạt động ổn định.
</objective>

<feature>
  <name>Quản lý Hồ sơ & Avatar</name>
  <files>code/apps/backend/src/controllers/user.controller.ts, code/apps/backend/src/services/user.service.ts, code/apps/backend/src/tests/profile.test.ts</files>
  <behavior>
    - Cập nhật hồ sơ: Nhận firstName, lastName, phone, bio -> Validate -> Lưu DB -> Trả về User.
    - Tải avatar: Nhận file -> Multer upload lên Cloudinary -> Lưu `avatarUrl` vào User -> Trả về URL.
    - Bảo mật: Chỉ User sở hữu tài khoản mới được cập nhật.
    - Test case 1: Cập nhật hợp lệ trả về 200.
    - Test case 2: SĐT sai định dạng trả về 400 (Tiếng Việt).
    - Test case 3: Upload ảnh thành công trả về URL ảnh.
  </behavior>
  <implementation>
    1. RED: Viết test case trong `profile.test.ts` cho 2 endpoint mới. Sử dụng `supertest` và mock `cloudinary`.
    2. GREEN: Implement `updateProfile` và `uploadAvatar` trong `user.service.ts` và `user.controller.ts`.
    3. Register routes trong `user.routes.ts` sử dụng `authMiddleware`.
    4. REFACTOR: Tối ưu code và đảm bảo coverage.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries
- Authenticated User → Profile Management API

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Elevation of Privilege | User Controller | mitigate | Verify `userId` from JWT matches the profile being updated. |
| T-02-02-02 | Information Disclosure | Error Messages | mitigate | Ensure internal DB errors are not leaked; use generic Vietnamese error messages. |
</threat_model>

<verification>
<automated>cd code/apps/backend && npm test src/tests/profile.test.ts</automated>
</verification>

<success_criteria>
1. Tất cả test cases trong `profile.test.ts` đều PASS.
2. Endpoint `PATCH /api/users/profile` trả về dữ liệu đã cập nhật.
3. Endpoint `POST /api/users/profile/avatar` trả về `avatarUrl` hợp lệ từ Cloudinary.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-ride/02-02-SUMMARY.md`
</output>
