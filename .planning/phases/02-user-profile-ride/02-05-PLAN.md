---
phase: 02-user-profile-ride
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - code/apps/web/src/app/rides/post/page.tsx
  - code/apps/web/src/app/rides/search/page.tsx
  - code/apps/web/src/components/rides/ride-card.tsx
  - code/apps/web/src/components/rides/search-form.tsx
autonomous: true
requirements: [RIDE-UI, SRCH-UI]

must_haves:
  truths:
    - "User có thể điền thông tin và đăng chuyến đi thành công"
    - "Hành khách có thể tìm thấy chuyến đi qua bộ lọc"
    - "Giao diện Tiếng Việt thân thiện, không dùng icon ngoài Lucide React"
  artifacts:
    - path: "code/apps/web/src/app/rides/post/page.tsx"
      provides: "Trang Đăng chuyến đi"
    - path: "code/apps/web/src/app/rides/search/page.tsx"
      provides: "Trang Tìm kiếm chuyến đi"
---

<objective>
Xây dựng giao diện cho hai tính năng quan trọng nhất của CoRide: Đăng chuyến (Driver) và Tìm chuyến (Passenger).

Output: Các trang `/rides/post` và `/rides/search` hoạt động tốt với Backend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@code/apps/web/src/lib/api-client.ts
@code/packages/shared/src/ride.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng Trang Đăng chuyến đi (Driver)</name>
  <files>code/apps/web/src/app/rides/post/page.tsx</files>
  <action>
    - Tạo form đăng chuyến sử dụng `createRideSchema` từ `@repo/shared`.
    - Input fields: Điểm đi, Điểm đến, Thời gian (DateTimePicker cơ bản), Số ghế, Giá tiền.
    - Submit gọi API `POST /api/rides`.
    - Redirect về trang chủ hoặc danh sách chuyến đi sau khi đăng thành công (có thông báo Tiếng Việt).
  </action>
  <verify>
    <automated>grep -q "api/rides" code/apps/web/src/app/rides/post/page.tsx</automated>
  </verify>
  <done>Trang Đăng chuyến đi hoạt động thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng Trang Tìm kiếm chuyến đi & Kết quả</name>
  <files>code/apps/web/src/app/rides/search/page.tsx, code/apps/web/src/components/rides/search-form.tsx, code/apps/web/src/components/rides/ride-card.tsx</files>
  <action>
    - Tạo `search-form.tsx` để lọc theo origin, destination, date.
    - Tạo `ride-card.tsx` để hiển thị tóm tắt chuyến đi (Điểm đi/đến, Thời gian, Giá, Driver Avatar).
    - Trang `/rides/search` thực hiện fetch `GET /api/rides?query...` khi form search submit.
    - Hiển thị danh sách kết quả hoặc thông báo "Không tìm thấy chuyến đi phù hợp" (Tiếng Việt).
  </action>
  <verify>
    <automated>ls code/apps/web/src/components/rides/ride-card.tsx</automated>
  </verify>
  <done>Trang Tìm kiếm chuyến đi hoạt động thành công.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- User → Ride Search & Post UI

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Post Ride Form | mitigate | Schema validation with Zod and date checks (future only). |
| T-02-05-02 | Information Disclosure | Driver Details | mitigate | Display only necessary driver info in search results (Name, Avatar, Rating). |
</threat_model>

<verification>
Thử đăng một chuyến đi mới và sau đó thực hiện tìm kiếm chuyến đi đó qua trang Search.
</verification>

<success_criteria>
1. Đăng chuyến đi thành công và lưu đúng dữ liệu vào database.
2. Tìm kiếm theo địa điểm hiển thị đúng kết quả mong muốn.
3. Toàn bộ UI bằng Tiếng Việt.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-ride/02-05-SUMMARY.md`
</output>
