---
phase: 02-user-profile-ride
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - code/apps/backend/src/controllers/ride.controller.ts
  - code/apps/backend/src/routes/ride.routes.ts
  - code/apps/backend/src/services/ride.service.ts
  - code/apps/backend/src/tests/ride.test.ts
  - code/apps/backend/src/server.ts
autonomous: true
requirements: [RIDE-01, SRCH-01]

must_haves:
  truths:
    - "Driver có thể đăng chuyến đi thành công"
    - "Passenger có thể tìm kiếm chuyến đi theo các bộ lọc"
    - "Logic CRUD và Search được kiểm thử đầy đủ"
  artifacts:
    - path: "code/apps/backend/src/tests/ride.test.ts"
      provides: "Unit & Integration tests cho Ride"
    - path: "code/apps/backend/src/controllers/ride.controller.ts"
      provides: "API endpoints cho Ride"
---

<objective>
Triển khai core API cho việc Đăng chuyến và Tìm kiếm chuyến đi sử dụng TDD. Đây là tính năng trung tâm của hệ thống CoRide.

Output: API `POST /api/rides` và `GET /api/rides` hoạt động ổn định và tin cậy.
</objective>

<feature>
  <name>Quản lý Chuyến đi (Ride Core)</name>
  <files>code/apps/backend/src/controllers/ride.controller.ts, code/apps/backend/src/services/ride.service.ts, code/apps/backend/src/tests/ride.test.ts</files>
  <behavior>
    - Đăng chuyến (POST /rides): Nhận data -> Validate (thời gian khởi hành phải ở tương lai) -> Lưu DB gắn với driverId (auth user).
    - Tìm kiếm (GET /rides): Hỗ trợ lọc theo `origin`, `destination`, `date`. Sử dụng `insensitive` match cho text.
    - Test case 1: Tạo ride hợp lệ trả về 201.
    - Test case 2: Tạo ride với ngày quá khứ trả về 400 (Tiếng Việt).
    - Test case 3: Tìm kiếm với params trả về danh sách rides phù hợp.
  </behavior>
  <implementation>
    1. RED: Viết test case trong `ride.test.ts` cho creation và search logic.
    2. GREEN: Implement `ride.service.ts` và `ride.controller.ts` sử dụng Prisma client.
    3. Tạo `ride.routes.ts` và đăng ký vào `server.ts` (path `/api/rides`).
    4. REFACTOR: Đảm bảo logic search hiệu quả và trả về thông tin driver đi kèm (include driver).
  </implementation>
</feature>

<threat_model>
## Trust Boundaries
- Authenticated User → Ride Management API

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Ride Creation | mitigate | Validate all fields using `createRideSchema` in Shared Schemas. |
| T-02-03-02 | Information Disclosure | Search Results | mitigate | Filter out sensitive Driver fields (like password) in the response. |
</threat_model>

<verification>
<automated>cd code/apps/backend && npm test src/tests/ride.test.ts</automated>
</verification>

<success_criteria>
1. Tất cả test cases trong `ride.test.ts` đều PASS.
2. Endpoint `POST /api/rides` tạo ride thành công trong DB.
3. Endpoint `GET /api/rides` trả về kết quả tìm kiếm chính xác dựa trên filters.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-ride/02-03-SUMMARY.md`
</output>
