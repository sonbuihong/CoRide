---
phase: 09-business-integration
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [code/apps/web/src/components/ui/address-autocomplete.tsx, code/apps/web/src/components/rides/search-form.tsx]
autonomous: true
requirements: [BIZ-01]

must_haves:
  truths:
    - "AddressAutocomplete hỗ trợ custom style cho input"
    - "SearchForm sử dụng AddressAutocomplete thay cho input text truyền thống"
  artifacts:
    - path: "code/apps/web/src/components/ui/address-autocomplete.tsx"
      provides: "AddressAutocomplete với khả năng styling linh hoạt"
    - path: "code/apps/web/src/components/rides/search-form.tsx"
      provides: "Giao diện tìm kiếm tích hợp Maps"
  key_links:
    - from: "code/apps/web/src/components/rides/search-form.tsx"
      to: "AddressAutocomplete"
      via: "Sử dụng component"
---

<objective>
Tinh chỉnh các component giao diện và tích hợp Autocomplete vào trang Tìm chuyến đi.

Mục tiêu: Đảm bảo trải nghiệm nhập liệu nhất quán và chuyên nghiệp theo phong cách Apple (Apple Design) trên toàn bộ ứng dụng.
Kết quả: AddressAutocomplete hỗ trợ styling tùy biến và SearchForm hoạt động mượt mà với dữ liệu Google Maps.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@code/apps/web/src/components/ui/address-autocomplete.tsx
@code/apps/web/src/components/rides/search-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Nâng cấp AddressAutocomplete</name>
  <files>code/apps/web/src/components/ui/address-autocomplete.tsx</files>
  <action>
    - Thêm prop `inputClassName?: string` vào `AddressAutocompleteProps`.
    - Truyền `inputClassName` vào component `Input` bên trong.
    - Cập nhật style của dropdown suggestions để khớp với thiết kế Apple (rounded, backdrop-blur nếu cần).
  </action>
  <verify>
    <automated>grep "inputClassName" code/apps/web/src/components/ui/address-autocomplete.tsx</automated>
  </verify>
  <done>AddressAutocomplete đã linh hoạt hơn về mặt giao diện</done>
</task>

<task type="auto">
  <name>Tích hợp Autocomplete vào SearchForm</name>
  <files>code/apps/web/src/components/rides/search-form.tsx</files>
  <action>
    - Import `AddressAutocomplete`.
    - Thay thế 2 ô input "Điểm đi" và "Điểm đến" bằng `AddressAutocomplete`.
    - Sử dụng `setValue` của `react-hook-form` để cập nhật giá trị khi người dùng chọn địa chỉ từ gợi ý.
    - Áp dụng `appleInputClass` vào `inputClassName` của `AddressAutocomplete`.
  </action>
  <verify>
    <automated>grep "AddressAutocomplete" code/apps/web/src/components/rides/search-form.tsx</automated>
  </verify>
  <done>Trang tìm kiếm đã hỗ trợ gợi ý địa chỉ từ Google</done>
</task>

</tasks>

<success_criteria>
Hành khách có thể tìm kiếm chuyến đi bằng cách chọn địa điểm từ gợi ý thay vì gõ text tự do.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-business-integration/09-02-SUMMARY.md`
</output>
