---
phase: 09-business-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/packages/database/prisma/schema.prisma, code/packages/shared/src/ride.schema.ts, code/apps/backend/src/modules/rides/rides.service.ts]
autonomous: true
requirements: [BIZ-02]

must_haves:
  truths:
    - "Model Ride có các trường lưu trữ distance và duration"
    - "Schema createRideSchema chấp nhận distance và duration"
    - "Backend service xử lý lưu trữ các trường mới này"
  artifacts:
    - path: "code/packages/database/prisma/schema.prisma"
      provides: "Trường distance và duration trong model Ride"
    - path: "code/packages/shared/src/ride.schema.ts"
      provides: "Validation cho distance và duration"
  key_links:
    - from: "code/apps/backend/src/modules/rides/rides.service.ts"
      to: "prisma.ride.create"
      via: "Lưu trữ distance/duration"
---

<objective>
Cập nhật cấu trúc dữ liệu để hỗ trợ lưu trữ Khoảng cách (Distance) và Thời gian di chuyển (Duration).

Mục tiêu: Đảm bảo hệ thống có khả năng lưu trữ và xử lý các tham số kỹ thuật của chuyến đi thu được từ Google Maps API.
Kết quả: Database được migrate, Schema được cập nhật, và Backend service sẵn sàng nhận dữ liệu mới.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@code/packages/database/prisma/schema.prisma
@code/packages/shared/src/ride.schema.ts
@code/apps/backend/src/modules/rides/rides.service.ts
</context>

<tasks>

<task type="auto">
  <name>Cập nhật Prisma Schema</name>
  <files>code/packages/database/prisma/schema.prisma</files>
  <action>
    Thêm các trường sau vào model `Ride`:
    - `distance`: Float? (Lưu khoảng cách tính bằng km)
    - `duration`: Int? (Lưu thời gian di chuyển tính bằng phút)
    Sau đó chạy lệnh migrate: `npx prisma migrate dev --name add_distance_duration_to_ride` trong thư mục database.
  </action>
  <verify>
    <automated>grep -E "distance|duration" code/packages/database/prisma/schema.prisma</automated>
  </verify>
  <done>Model Ride trong database đã có trường distance và duration</done>
</task>

<task type="auto" tdd="true">
  <name>Cập nhật Shared Schema</name>
  <files>code/packages/shared/src/ride.schema.ts</files>
  <behavior>
    - createRideSchema phải chấp nhận distance (number, optional)
    - createRideSchema phải chấp nhận duration (number, optional)
  </behavior>
  <action>
    Cập nhật `createRideSchema` trong `ride.schema.ts` để bao gồm `distance` và `duration`. Sử dụng `.optional()` vì các trường này được tính toán tự động sau khi chọn địa điểm.
  </action>
  <verify>
    <automated>grep -E "distance|duration" code/packages/shared/src/ride.schema.ts</automated>
  </verify>
  <done>Zod schema đã sẵn sàng cho dữ liệu distance/duration</done>
</task>

<task type="auto">
  <name>Cập nhật Backend Service</name>
  <files>code/apps/backend/src/modules/rides/rides.service.ts</files>
  <action>
    Cập nhật các phương thức `createRide` và `updateRide` trong `RidesService` để nhận và lưu trữ `distance` và `duration` vào database.
  </action>
  <verify>
    <automated>grep -E "distance|duration" code/apps/backend/src/modules/rides/rides.service.ts</automated>
  </verify>
  <done>Backend service đã có thể lưu trữ dữ liệu bản đồ</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend -> Backend | Dữ liệu distance/duration từ Client có thể bị thao túng |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Create Ride API | mitigate | Backend lưu trữ nhưng chỉ dùng cho hiển thị, không dùng làm căn cứ thanh toán trực tiếp ở phase này. Cần cân nhắc re-calculate ở backend trong tương lai. |
</threat_model>

<success_criteria>
Hệ thống sẵn sàng về mặt dữ liệu để lưu trữ thông tin lộ trình từ Google Maps.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-business-integration/09-01-SUMMARY.md`
</output>
