---
phase: 09-business-integration
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [code/apps/web/src/components/ui/map-viewer.tsx, code/apps/web/src/app/rides/post/page.tsx]
autonomous: true
requirements: [BIZ-01, BIZ-02]

must_haves:
  truths:
    - "Bản đồ hiển thị trên trang Đăng chuyến đi ngay khi chọn địa điểm"
    - "Khoảng cách và thời gian tự động được tính toán và hiển thị"
    - "Các thông số này được gửi lên server khi đăng chuyến đi"
  artifacts:
    - path: "code/apps/web/src/components/ui/map-viewer.tsx"
      provides: "Callback báo cáo lộ trình (distance/duration)"
    - path: "code/apps/web/src/app/rides/post/page.tsx"
      provides: "Giao diện đăng chuyến đi thông minh"
  key_links:
    - from: "code/apps/web/src/app/rides/post/page.tsx"
      to: "MapViewer"
      via: "Hiển thị lộ trình và lấy dữ liệu tính toán"
---

<objective>
Hợp nhất Bản đồ vào quy trình Đăng chuyến đi và tự động hóa tính toán lộ trình.

Mục tiêu: Loại bỏ việc nhập thủ công các thông số kỹ thuật, đảm bảo tính chính xác và cung cấp trải nghiệm trực quan cho Tài xế.
Kết quả: Tài xế thấy được lộ trình trên bản đồ và các thông số khoảng cách/thời gian tự động điền vào form.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@code/apps/web/src/components/ui/map-viewer.tsx
@code/apps/web/src/app/rides/post/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Thêm Callback vào MapViewer</name>
  <files>code/apps/web/src/components/ui/map-viewer.tsx</files>
  <action>
    - Thêm `onRouteCalculated?: (data: { distance: number; duration: number }) => void` vào `MapViewerProps`.
    - Trong `useEffect` tính toán route, khi nhận được kết quả `OK`, gọi `onRouteCalculated` với:
        - `distance`: `result.routes[0].legs[0].distance.value / 1000` (đổi sang km).
        - `duration`: `Math.ceil(result.routes[0].legs[0].duration.value / 60)` (đổi sang phút).
  </action>
  <verify>
    <automated>grep "onRouteCalculated" code/apps/web/src/components/ui/map-viewer.tsx</automated>
  </verify>
  <done>MapViewer có khả năng thông báo kết quả tính toán lộ trình ra bên ngoài</done>
</task>

<task type="auto">
  <name>Tích hợp Bản đồ vào Trang Đăng chuyến</name>
  <files>code/apps/web/src/app/rides/post/page.tsx</files>
  <action>
    - Import `MapViewer`.
    - Hiển thị `MapViewer` trong giao diện (ví dụ: phía dưới hoặc bên cạnh phần nhập địa chỉ).
    - Quản lý state cho `originLoc` và `destinationLoc` ({lat, lng}).
    - Khi người dùng chọn địa chỉ qua `AddressAutocomplete`, cập nhật `originLoc`/`destinationLoc`.
    - Sử dụng callback `onRouteCalculated` từ `MapViewer` để cập nhật `distance` và `duration` vào form (`setValue`).
    - Hiển thị thông tin Khoảng cách và Thời gian dự kiến lên giao diện (read-only) để người dùng xác nhận.
  </action>
  <verify>
    <automated>grep "MapViewer" code/apps/web/src/app/rides/post/page.tsx</automated>
  </verify>
  <done>Tài xế đã có thể thấy lộ trình và các thông số tự động khi đăng chuyến</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Toàn bộ luồng đăng chuyến đi tích hợp Maps và Autocomplete</what-built>
  <how-to-verify>
    1. Truy cập trang Đăng chuyến đi.
    2. Chọn Điểm đi và Điểm đến từ gợi ý Google.
    3. Kiểm tra xem Bản đồ có hiện lộ trình (vạch xanh) không.
    4. Kiểm tra xem Khoảng cách và Thời gian có tự động hiện ra không.
    5. Đăng chuyến đi và kiểm tra trong database (hoặc trang My Rides) xem các thông số đã được lưu chưa.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
Quy trình đăng chuyến đi hoàn toàn tự động hóa phần địa điểm và lộ trình.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-business-integration/09-03-SUMMARY.md`
</output>
