---
phase: 10-realtime-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/apps/backend/src/shared/socket/socket.ts
  - code/apps/backend/src/server.ts
  - code/apps/backend/src/modules/bookings/booking.service.ts
  - code/apps/web/src/components/providers/socket-provider.tsx
  - code/apps/web/src/components/layout/notification-center.tsx
autonomous: true
requirements: [REAL-01, REAL-02]
must_haves:
  truths:
    - Log Backend xác nhận "Socket initialized" khi khởi động.
    - Console Frontend hiển thị "[Socket] Connected" kèm theo userId sau khi đăng nhập.
  artifacts:
    - path: code/apps/backend/src/shared/socket/socket.ts
      provides: Singleton instance của Socket.IO Server
      min_lines: 30
    - path: code/apps/web/src/components/providers/socket-provider.tsx
      provides: React Context Provider cho Socket.IO Client
      min_lines: 40
  key_links:
    - from: code/apps/backend/src/server.ts
      to: code/apps/backend/src/shared/socket/socket.ts
      via: initSocket(server)
    - from: code/apps/web/src/app/layout.tsx
      to: code/apps/web/src/components/providers/socket-provider.tsx
      via: <SocketProvider> wrapper
    - from: code/apps/backend/src/modules/bookings/booking.service.ts
      to: code/apps/backend/src/shared/socket/socket.ts
      via: getIO().emit()
---

<objective>
Hoàn thiện và kiểm chứng hạ tầng Socket.IO cùng hệ thống thông báo đẩy (Push Notifications).
Đảm bảo Backend có thể gửi và Frontend có thể nhận thông báo ngay lập tức khi có sự kiện đặt chuyến.

Mục tiêu:
- Hạ tầng Socket.IO hoạt động ổn định, có xác thực JWT.
- Khi có Booking mới hoặc cập nhật trạng thái, User liên quan nhận được Toast ngay lập tức.
</objective>

<execution_context>
Hạ tầng cơ bản đã có mã nguồn sơ bộ, cần kiểm tra tính đầy đủ và wiring chính xác.
@code/apps/backend/src/shared/socket/socket.ts
@code/apps/web/src/components/providers/socket-provider.tsx
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-realtime-core/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Kiểm tra và hoàn thiện cấu hình Socket.IO Backend</name>
  <files>code/apps/backend/src/shared/socket/socket.ts, code/apps/backend/src/server.ts</files>
  <action>
    - Đảm bảo `socket.ts` đã thực hiện xác thực JWT qua handshake.auth.token (thông tin xác thực truyền qua bắt tay kết nối) bằng thư viện `jose`.
    - Đảm bảo mỗi user khi kết nối sẽ tự động `join` (tham gia) vào room (phòng) có tên là `userId`.
    - Xác nhận `server.ts` đã bọc Express app bằng `http.createServer` và gọi `initSocket`.
    - Nếu mã nguồn đã có, thực hiện kiểm tra lỗi syntax (ngữ pháp code) và logic singleton (khởi tạo duy nhất).
  </action>
  <verify>
    <automated>npm --prefix code/apps/backend run build</automated>
  </verify>
  <done>Backend khởi tạo Socket.IO thành công không lỗi.</done>
</task>

<task type="auto">
  <name>Kiểm tra và hoàn thiện SocketProvider Frontend</name>
  <files>code/apps/web/src/components/providers/socket-provider.tsx, code/apps/web/src/app/layout.tsx</files>
  <action>
    - Đảm bảo `SocketProvider` lấy `accessToken` từ localStorage để kết nối.
    - Đảm bảo có xử lý `reconnection` (tự động kết nối lại) và `cleanup` (dọn dẹp bộ nhớ/disconnect) khi `unmount` (component bị gỡ khỏi giao diện).
    - Xác nhận `RootLayout` đã bọc toàn bộ ứng dụng bằng `SocketProvider` (nằm trong `AuthProvider`).
  </action>
  <verify>
    <automated>npm --prefix code/apps/web run build</automated>
  </verify>
  <done>Frontend có SocketContext hoạt động, tự động kết nối khi đăng nhập.</done>
</task>

<task type="auto">
  <name>Kích hoạt thông báo đẩy trong NotificationCenter</name>
  <files>code/apps/web/src/components/layout/notification-center.tsx</files>
  <action>
    - Đảm bảo `NotificationCenter` lắng nghe sự kiện `notification:new` từ socket.
    - Khi có thông báo mới: Cập nhật state list (danh sách trạng thái), tăng số unread (chưa đọc) và hiển thị `toast.info` (sonner).
    - Đảm bảo có cơ chế fallback (dự phòng) nếu socket không kết nối được (sử dụng mặc định Long Polling của Socket.IO).
  </action>
  <verify>
    <automated>grep -q "notification:new" code/apps/web/src/components/layout/notification-center.tsx</automated>
  </verify>
  <done>Người dùng nhận được Toast và cập nhật badge khi có thông báo mới qua Socket.</done>
</task>

<task type="auto">
  <name>Tích hợp gửi thông báo từ BookingService</name>
  <files>code/apps/backend/src/modules/bookings/booking.service.ts</files>
  <action>
    - Import `getIO` từ `shared/socket/socket`.
    - Tìm hàm xử lý tạo Booking mới (`createBooking`) và cập nhật trạng thái (`updateBookingStatus`).
    - Thêm lệnh `getIO().to(targetUserId).emit('notification:new', { message: '...' })` để gửi thông báo cho Tài xế (khi có khách đặt) hoặc cho Hành khách (khi tài xế xác nhận).
  </action>
  <verify>
    <automated>grep -q "getIO" code/apps/backend/src/modules/bookings/booking.service.ts</automated>
  </verify>
  <done>Hệ thống nghiệp vụ đã có thể đẩy dữ liệu thông báo sang tầng Realtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Socket.IO | Untrusted connections from browser to websocket server |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Socket Authentication | mitigate | Xác thực JWT trong middleware `io.use()` trước khi cho phép kết nối. |
| T-10-01-02 | Information Disclosure | Room Isolation | mitigate | Chỉ cho phép user join vào room của chính họ (`userId`) để đảm bảo không nhận nhầm thông báo của người khác. |
</threat_model>

<verification>
Chạy cả Backend và Frontend, thực hiện Đặt chuyến đi và kiểm tra xem Tài xế có nhận được Toast thông báo ngay lập tức không.
</verification>

<success_criteria>
1. Log Backend hiển thị `[Socket] Connected: user ...`.
2. Khi Booking được tạo, Tài xế nhận được Toast "Yêu cầu đặt chỗ mới" trên màn hình.
3. Badge thông báo tăng số lượng mà không cần F5 trang.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/10-realtime-core/10-01-SUMMARY.md`
</output>
