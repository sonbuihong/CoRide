---
phase: 10-realtime-core
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - code/packages/database/prisma/schema.prisma
  - code/apps/backend/src/modules/chat/chat.service.ts
  - code/apps/backend/src/modules/chat/chat.controller.ts
  - code/apps/backend/src/modules/chat/chat.router.ts
  - code/apps/backend/src/app.ts
autonomous: true
requirements: [REAL-03]
must_haves:
  truths:
    - Lệnh `npx prisma migrate status` xác nhận migration thành công.
    - API `GET /api/chat/:rideId/:userId` trả về mã trạng thái 200.
  artifacts:
    - path: code/packages/database/prisma/schema.prisma
      provides: Định nghĩa bảng Message trong Database
      min_lines: 100
    - path: code/apps/backend/src/modules/chat/chat.service.ts
      provides: Logic xử lý lưu và lấy lịch sử tin nhắn
      min_lines: 40
  key_links:
    - from: code/apps/backend/src/app.ts
      to: code/apps/backend/src/modules/chat/chat.router.ts
      via: app.use('/api/chat', chatRouter)
---

<objective>
Thiết lập nền tảng dữ liệu và API cho tính năng chat giữa Tài xế và Hành khách.
Xây dựng Model Message và các endpoint để lấy lịch sử trò chuyện.

Mục tiêu:
- Có bảng `Message` trong database để lưu trữ tin nhắn.
- Có API lấy danh sách tin nhắn giữa 2 người dùng trong một chuyến đi.
</objective>

<execution_context>
Cần cập nhật Prisma schema và chạy migration. Backend sẽ thêm module `chat` mới.
</execution_context>

<context>
@code/packages/database/prisma/schema.prisma
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Cập nhật Prisma Schema cho Model Message</name>
  <files>code/packages/database/prisma/schema.prisma</files>
  <action>
    - Thêm model `Message` vào schema:
      ```prisma
      model Message {
        id        String   @id @default(uuid())
        rideId    String
        ride      Ride     @relation(fields: [rideId], references: [id], onDelete: Cascade)
        senderId  String
        sender    User     @relation("SentMessages", fields: [senderId], references: [id])
        receiverId String
        receiver   User     @relation("ReceivedMessages", fields: [receiverId], references: [id])
        content   String
        isRead    Boolean  @default(false)
        createdAt DateTime @default(now())
      }
      ```
    - Cập nhật model `User` để thêm các relation tương ứng (`sentMessages`, `receivedMessages`).
    - Cập nhật model `Ride` để thêm relation `messages`.
    - Chạy lệnh `npx prisma generate` và `npx prisma migrate dev --name add_message_model`.
  </action>
  <verify>
    <automated>npx prisma validate --schema code/packages/database/prisma/schema.prisma</automated>
  </verify>
  <done>Database có bảng Message với các quan hệ chính xác.</done>
</task>

<task type="auto">
  <name>Xây dựng Chat Module Backend</name>
  <files>code/apps/backend/src/modules/chat/chat.service.ts, code/apps/backend/src/modules/chat/chat.controller.ts, code/apps/backend/src/modules/chat/chat.router.ts, code/apps/backend/src/app.ts</files>
  <action>
    - Tạo `ChatService` với hàm `getChatHistory(rideId, userA, userB)` để lấy tin nhắn qua lại.
    - Tạo `ChatController` để xử lý request từ client.
    - Tạo `ChatRouter` đăng ký endpoint `GET /chat/:rideId/:otherUserId`.
    - Đăng ký `ChatRouter` vào `app.ts`.
  </action>
  <verify>
    <automated>npm --prefix code/apps/backend run build</automated>
  </verify>
  <done>Backend cung cấp API lấy lịch sử chat giữa tài xế và hành khách.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API Endpoint | Validation of message ownership |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | Chat History | mitigate | Kiểm tra `rideId` và `userId` trong API để đảm bảo người dùng chỉ xem được chat mà họ tham gia. |
| T-10-02-02 | Tampering | Message Content | mitigate | Validate nội dung tin nhắn không trống và không quá dài qua Zod. |
</threat_model>

<verification>
Sử dụng Postman hoặc curl gọi API `GET /api/chat/:rideId/:userId` và xác nhận dữ liệu trả về đúng cấu trúc (mảng rỗng nếu chưa có chat).
</verification>

<success_criteria>
1. Prisma schema hợp lệ và đã migrate thành công.
2. Endpoint `/api/chat` trả về dữ liệu lịch sử tin nhắn.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/10-realtime-core/10-02-SUMMARY.md`
</output>
