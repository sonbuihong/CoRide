---
phase: 10-realtime-core
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - code/apps/backend/src/shared/socket/socket.ts
  - code/apps/web/src/components/chat/chat-window.tsx
  - code/apps/web/src/app/rides/[id]/page.tsx
autonomous: false
requirements: [REAL-03]
must_haves:
  truths:
    - Tin nhắn gửi từ tab A xuất hiện trên tab B không cần tải lại trang.
    - Refresh trang (tải lại trang) -> Nội dung tin nhắn vẫn được hiển thị chính xác từ Database.
  artifacts:
    - path: code/apps/web/src/components/chat/chat-window.tsx
      provides: Giao diện và logic gửi nhận tin nhắn Real-time
      min_lines: 80
  key_links:
    - from: code/apps/web/src/components/chat/chat-window.tsx
      to: code/apps/backend/src/shared/socket/socket.ts
      via: socket.emit('chat:send')
---

<objective>
Tích hợp tính năng nhắn tin thời gian thực qua Socket.IO và xây dựng giao diện trò chuyện cho người dùng.
Hoàn tất mục tiêu liên lạc trực tiếp giữa Tài xế và Hành khách.

Mục tiêu:
- Gửi và nhận tin nhắn ngay lập tức (Real-time).
- Giao diện chat trực quan tích hợp vào trang chi tiết chuyến đi/booking.
</objective>

<execution_context>
Sử dụng Socket.IO events đã thiết lập ở hạ tầng. Cần bọc logic lắng nghe tin nhắn vào một component Chat chuyên dụng.
</execution_context>

<context>
@code/apps/backend/src/shared/socket/socket.ts
@code/apps/web/src/components/providers/socket-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Tích hợp sự kiện Chat vào Socket.IO Backend</name>
  <files>code/apps/backend/src/shared/socket/socket.ts</files>
  <action>
    - Bổ sung listener sự kiện `chat:send` trong `io.on('connection')`.
    - Khi nhận `chat:send`:
      1. Lưu tin nhắn vào Database qua `ChatService`.
      2. Emit sự kiện `chat:receive` tới room của người nhận (`receiverId`).
    - (Tùy chọn) Emit sự kiện `chat:typing` để hiển thị trạng thái đang nhập.
  </action>
  <verify>
    <automated>grep -q "chat:send" code/apps/backend/src/shared/socket/socket.ts</automated>
  </verify>
  <done>Backend xử lý chuyển tiếp tin nhắn thời gian thực giữa 2 người dùng.</done>
</task>

<task type="auto">
  <name>Xây dựng giao diện ChatWindow Frontend</name>
  <files>code/apps/web/src/components/chat/chat-window.tsx</files>
  <action>
    - Tạo component `ChatWindow` hiển thị danh sách tin nhắn và ô nhập text.
    - Sử dụng hook `useSocket()` để:
      1. Gửi tin nhắn qua `socket.emit('chat:send', ...)`.
      2. Lắng nghe tin nhắn mới qua `socket.on('chat:receive', ...)`.
    - Gọi API lấy lịch sử chat khi mở cửa sổ lần đầu.
    - Tự động cuộn xuống dưới cùng khi có tin nhắn mới.
  </action>
  <verify>
    <automated>npm --prefix code/apps/web run build</automated>
  </verify>
  <done>Giao diện chat hoạt động với Socket.IO.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Toàn bộ tính năng Chat thời gian thực</what-built>
  <how-to-verify>
    1. Mở 2 trình duyệt (ẩn danh và bình thường), đăng nhập bằng 2 account khác nhau (Tài xế và Hành khách).
    2. Hành khách vào trang Booking của chuyến đi đó.
    3. Mở cửa sổ chat và gửi tin nhắn.
    4. Kiểm tra xem Tài xế có nhận được tin nhắn ngay lập tức mà không cần F5 trang không.
    5. F5 lại trang để đảm bảo tin nhắn vẫn còn (đã lưu DB).
  </how-to-verify>
  <resume-signal>Gửi tin nhắn realtime thành công giữa 2 account.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Socket Event | Authorization of message sender |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | Chat Sender | mitigate | Backend lấy `senderId` từ `socket.data.userId` (đã xác thực qua JWT) thay vì tin tưởng vào dữ liệu client gửi lên. |
| T-10-03-02 | Information Disclosure | Chat Eavesdropping | mitigate | Chỉ emit `chat:receive` tới đúng room `receiverId`. |
</threat_model>

<verification>
Kiểm tra luồng gửi nhận tin nhắn giữa 2 tab trình duyệt khác nhau.
</verification>

<success_criteria>
1. Tin nhắn gửi đi xuất hiện tức thì bên máy người nhận.
2. Có thông báo Toast nếu người nhận đang ở trang khác (Optional).
3. Lịch sử chat được lưu trữ và hiển thị đúng sau khi tải lại trang.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/10-realtime-core/10-03-SUMMARY.md`
</output>
