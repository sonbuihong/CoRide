---
phase: 11-payment-gateway
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: [code/apps/mobile/src/screens/BookingDetailScreen.tsx, code/apps/web/src/pages/admin/Transactions.tsx, code/apps/web/src/components/AdminLayout.tsx, code/apps/backend/src/modules/payments/payments.controller.ts, code/apps/backend/src/modules/payments/payments.router.ts]
autonomous: false
requirements: [PAY-03, PAY-02]

must_haves:
  truths:
    - "Người dùng có thể nhấn 'Thanh toán' và được chuyển hướng sang trang ZaloPay"
    - "Admin có thể xem danh sách lịch sử tất cả giao dịch trong hệ thống"
    - "Giao diện hiển thị đúng định dạng tiền tệ Việt Nam (đ)"
  artifacts:
    - path: "code/apps/mobile/src/screens/BookingDetailScreen.tsx"
      provides: "Nút thanh toán và xử lý redirect"
    - path: "code/apps/web/src/pages/admin/Transactions.tsx"
      provides: "Dashboard lịch sử giao dịch"
  key_links:
    - from: "Mobile App"
      to: "Backend /api/payments/create"
      via: "POST request"
    - from: "Web App (Admin)"
      to: "Backend /api/payments/transactions"
      via: "GET request"
---

<objective>
Hoàn thiện trải nghiệm người dùng cuối bằng cách tích hợp nút thanh toán vào ứng dụng di động và xây dựng trang quản trị giao dịch cho Admin.

Mục tiêu:
- Passenger có thể thanh toán cho chuyến đi của mình.
- Admin có thể giám sát toàn bộ dòng tiền trong hệ thống.
</objective>

<execution_context>
@code/apps/mobile/app/booking/[id].tsx
@code/apps/web/src/app/admin/layout.tsx
</execution_context>

<context>
@.planning/phases/11-payment-gateway/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tích hợp nút Thanh toán trên Mobile</name>
  <files>code/apps/mobile/src/screens/BookingDetailScreen.tsx</files>
  <action>
    1. Kiểm tra trạng thái Booking: Nếu là CONFIRMED và UNPAID -> Hiển thị nút "Thanh toán bằng ZaloPay".
    2. Khi nhấn nút:
       - Gọi API `POST /api/payments/create`.
       - Nhận về `order_url`.
       - Sử dụng `Linking.openURL(order_url)` (React Native) để mở trang thanh toán của ZaloPay.
    3. Xử lý logic quay lại app sau khi thanh toán (Return URL).
  </action>
  <verify>
    <automated>npm --prefix code/apps/mobile run lint</automated>
  </verify>
  <done>Nút thanh toán hoạt động, chuyển hướng sang ZaloPay thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng Dashboard Giao dịch (Admin)</name>
  <files>code/apps/web/src/pages/admin/Transactions.tsx, code/apps/web/src/components/AdminLayout.tsx, code/apps/backend/src/modules/payments/payments.controller.ts, code/apps/backend/src/modules/payments/payments.router.ts</files>
  <action>
    1. Tạo trang `Transactions.tsx` hiển thị bảng danh sách giao dịch:
       - Các cột: ID, Người dùng, Loại (Nạp/Thanh toán...), Số tiền, Trạng thái, Thời gian.
       - Định dạng tiền tệ sử dụng `Intl.NumberFormat('vi-VN')`.
    2. Thêm menu "Giao dịch" vào Admin Layout.
    3. Xây dựng API endpoint `GET /api/payments/admin/transactions` (chỉ dành cho Admin) để lấy dữ liệu.
  </action>
  <verify>
    <automated>npm --prefix code/apps/web run build</automated>
  </verify>
  <done>Admin có thể xem lịch sử giao dịch đầy đủ.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kiểm thử luồng Thanh toán End-to-End</name>
  <files>code/apps/backend/.env</files>
  <action>
    Xác nhận luồng thanh toán hoạt động bằng cách:
    1. Chạy ngrok để nhận callback: `ngrok http 3001`.
    2. Cập nhật `ZALOPAY_CALLBACK_URL` trong .env backend dựa trên URL của ngrok.
    3. Dùng app Mobile đặt chuyến -> Tài xế xác nhận -> Passenger nhấn Thanh toán.
    4. Hoàn tất thanh toán tại trang giả lập ZaloPay Sandbox.
    5. Kiểm tra Booking chuyển sang PAID trên app.
    6. Đăng nhập Admin trên Web, kiểm tra Giao dịch mới xuất hiện trong Dashboard.
  </action>
  <verify>approved</verify>
  <done>Luồng thanh toán End-to-End hoạt động chính xác.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → API | Admin truy cập dữ liệu nhạy cảm qua Dashboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information Disclosure | Admin Dashboard | mitigate | BẮT BUỘC kiểm tra quyền `Role.ADMIN` trước khi trả dữ liệu giao dịch từ API. |
| T-11-07 | Tampering | Pay Now button | mitigate | Đảm bảo nút thanh toán chỉ hiển thị khi Booking thuộc về chính User đó. |
</threat_model>

<verification>
Kiểm tra phân quyền API admin/transactions và xác nhận luồng thanh toán mobile.
</verification>

<success_criteria>
1. Luồng thanh toán từ Mobile -> Sandbox -> Backend Callback hoạt động trơn tru.
2. Dashboard Admin hiển thị chính xác các giao dịch vừa thực hiện.
</success_criteria>

<output>
After completion, create `.planning/phases/11-payment-gateway/11-03-SUMMARY.md`
</output>
ent-gateway/11-03-SUMMARY.md`
</output>
