---
phase: "01"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified: [
  "code/apps/web/src/app/layout.tsx",
  "code/apps/web/src/app/page.tsx",
  "code/apps/web/src/app/login/page.tsx",
  "code/apps/web/src/app/register/page.tsx",
  "code/apps/web/src/components/auth/auth-form.tsx",
  "code/apps/web/src/lib/api-client.ts"
]
autonomous: false
requirements: ["REQ-UI-01", "REQ-AUTH-01"]

must_haves:
  truths:
    - "Trang chủ, Đăng nhập và Đăng ký hiển thị bằng tiếng Việt 100%"
    - "Người dùng có thể đăng ký và đăng nhập trực tiếp từ giao diện Web"
    - "Giao diện phản hồi tốt (Responsive) và sử dụng Tailwind CSS"
    - "Dữ liệu form được validate bằng shared Zod schemas"
  artifacts:
    - path: "code/apps/web/src/app/login/page.tsx"
      provides: "Trang đăng nhập tiếng Việt"
    - path: "code/apps/web/src/lib/api-client.ts"
      provides: "Axios client với interceptor xử lý 401"
  key_links:
    - from: "auth-form.tsx"
      to: "/api/auth"
      via: "Axios POST request"
---

<objective>
Xây dựng giao diện Web cho hệ thống xác thực của CoRide.
Mục tiêu:
1. Thiết kế trang Home, Login, Register với ngôn ngữ Tiếng Việt 100%.
2. Tích hợp shared Zod schemas để validate form phía client.
3. Kết nối với Backend API để thực hiện đăng ký/đăng nhập.
4. Xử lý trạng thái xác thực và lưu trữ token (in-memory/cookie).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thiết lập Layout và API Client</name>
  <files>code/apps/web/src/app/layout.tsx, code/apps/web/src/lib/api-client.ts</files>
  <action>
    - Cập nhật layout.tsx với font chữ và metadata tiếng Việt.
    - Cấu hình Axios client trong api-client.ts với `withCredentials: true`.
    - Thêm interceptor để tự động thử lại request khi gặp lỗi 401 (gọi /refresh).
  </action>
  <verify>
    <automated>cd code/apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Nền tảng giao diện và kết nối API sẵn sàng.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng trang Đăng ký và Đăng nhập (Tiếng Việt)</name>
  <files>code/apps/web/src/app/register/page.tsx, code/apps/web/src/app/login/page.tsx, code/apps/web/src/app/page.tsx, code/apps/web/src/components/auth/auth-form.tsx</files>
  <action>
    - Tạo Form đăng ký sử dụng `react-hook-form` và `@hookform/resolvers/zod` với shared RegisterSchema.
    - Tạo Form đăng nhập với shared LoginSchema.
    - Đảm bảo tất cả Label, Placeholder, Error message đều là Tiếng Việt.
    - Cập nhật trang chủ (page.tsx) hiển thị lời chào Tiếng Việt và nút Đăng nhập/Đăng ký.
  </action>
  <verify>
    <automated>npm run lint --filter=web</automated>
  </verify>
  <done>Giao diện xác thực hoàn thiện và đã được Việt hóa.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Kiểm chứng luồng xác thực thực tế</name>
  <files>code/apps/web/src/app/login/page.tsx</files>
  <action>Kiểm tra thủ công sự tích hợp giữa Frontend và Backend sau khi tự động hóa hoàn thành.</action>
  <verify>Kiểm tra các bước dưới đây bằng trình duyệt.</verify>
  <done>Người dùng có thể đăng nhập/đăng ký thành công bằng tiếng Việt.</done>
  <what-built>Toàn bộ luồng đăng ký/đăng nhập từ Web tới Backend và DB.</ what-built>
  <how-to-verify>
    1. Truy cập trang /register, tạo tài khoản mới.
    2. Kiểm tra thông báo thành công tiếng Việt.
    3. Chuyển sang /login, đăng nhập bằng tài khoản vừa tạo.
    4. Kiểm tra xem có chuyển hướng về trang chủ thành công không.
    5. Kiểm tra cookie trong trình duyệt (HttpOnly Refresh Token).
  </how-to-verify>
  <resume-signal>Giao diện tiếng Việt chuẩn, login/register hoạt động mượt mà.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web -> Browser Storage | Lưu trữ Access Token (In-memory) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Client-side Validation | mitigate | Luôn luôn validate lại ở Backend bằng cùng Zod Schema |
</threat_model>

<verification>
Kiểm tra trang web tại http://localhost:3000. Đảm bảo không có từ tiếng Anh nào trong giao diện người dùng chính.
</verification>

<success_criteria>
- Trang chủ hiển thị lời chào tiếng Việt.
- Login/Register hoạt động và thông báo lỗi bằng tiếng Việt.
- Token được quản lý đúng cách (Refresh Token trong cookie).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
