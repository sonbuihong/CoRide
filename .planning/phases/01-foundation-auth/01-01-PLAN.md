---
phase: "01"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: [
  "code/package.json",
  "code/turbo.json",
  "code/packages/database/package.json",
  "code/packages/database/prisma/schema.prisma",
  "code/packages/database/src/index.ts",
  "code/packages/shared/package.json",
  "code/packages/shared/src/index.ts",
  "code/packages/shared/src/auth.schema.ts"
]
autonomous: true
requirements: ["REQ-INF-01", "REQ-DB-01"]

must_haves:
  truths:
    - "Cấu trúc Monorepo được thiết lập với Turborepo"
    - "Database schema bao gồm các bảng User, Ride, Booking, Review"
    - "Shared package cung cấp các Zod schema cho xác thực"
  artifacts:
    - path: "code/turbo.json"
      provides: "Cấu hình build pipeline"
    - path: "code/packages/database/prisma/schema.prisma"
      provides: "Database model hoàn chỉnh"
    - path: "code/packages/shared/src/auth.schema.ts"
      provides: "Zod validation cho login/register"
  key_links:
    - from: "apps/backend"
      to: "packages/database"
      via: "npm workspace dependency"
    - from: "apps/web"
      to: "packages/shared"
      via: "npm workspace dependency"
---

<objective>
Thiết lập nền tảng Monorepo và Database cho dự án CoRide.
Mục tiêu:
1. Cấu hình Turborepo để quản lý các ứng dụng và package.
2. Xây dựng Database Schema hoàn chỉnh với Prisma phục vụ các tính năng cốt lõi.
3. Tạo package shared để dùng chung logic validation (Zod) giữa Backend và Web.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thiết lập cấu trúc Monorepo với Turborepo</name>
  <files>code/package.json, code/turbo.json, code/packages/database/package.json, code/packages/shared/package.json</files>
  <action>
    - Khởi tạo root package.json với cấu hình workspaces cho apps/* và packages/*.
    - Cấu hình turbo.json với các pipeline: build, lint, dev.
    - Tạo thư mục packages/database và packages/shared với file package.json tương ứng.
    - Cài đặt các dependency cần thiết tại root (turbo, typescript, zod).
  </action>
  <verify>
    <automated>npx turbo run build --filter=@repo/shared</automated>
  </verify>
  <done>Cấu trúc thư mục Monorepo sẵn sàng, turbo run build thành công cho các package cơ sở.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng Database Schema với Prisma</name>
  <files>code/packages/database/prisma/schema.prisma, code/packages/database/src/index.ts</files>
  <action>
    - Di chuyển hoặc khởi tạo schema.prisma trong packages/database/prisma/.
    - Định nghĩa các Model: User (Email, Password, Role, Rating), Ride (Driver, Origin, Destination, Seats, Price, Status), Booking (Ride, Passenger, Seats, Status), Review (Ride, Reviewer, Reviewee, Rating).
    - Cấu hình Prisma Client output sang thư mục generated/client.
    - Tạo Singleton Prisma instance trong src/index.ts để export cho các apps sử dụng.
    - Thực hiện prisma generate.
  </action>
  <verify>
    <automated>cd code/packages/database && npx prisma validate</automated>
  </verify>
  <done>Database schema hợp lệ, Prisma Client được generate thành công.</done>
</task>

<task type="auto">
  <name>Task 3: Triển khai Shared Validation Schemas</name>
  <files>code/packages/shared/src/auth.schema.ts, code/packages/shared/src/index.ts</files>
  <action>
    - Tạo Zod schema cho Register (email, password, firstName, lastName, phone) và Login (email, password) trong auth.schema.ts.
    - Export các schema và các Type tương ứng (infer từ Zod) trong index.ts.
    - Đảm bảo các quy tắc validation (độ dài mật khẩu, định dạng email, số điện thoại Việt Nam).
  </action>
  <verify>
    <automated>cd code/packages/shared && npx tsc --noEmit</automated>
  </verify>
  <done>Shared types và validation schemas sẵn sàng cho Backend và Frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Input từ người dùng qua Zod schemas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Shared Schemas | mitigate | Chốt schema chặt chẽ với Zod, validate cả ở Frontend và Backend |
</threat_model>

<verification>
Kiểm tra cấu trúc Monorepo: `ls apps/ packages/` và lệnh `npx turbo run build` phải hoạt động.
</verification>

<success_criteria>
- Monorepo hoạt động ổn định với Turborepo.
- Prisma schema phản ánh đúng yêu cầu nghiệp vụ carpooling.
- Các package shared có thể được import vào apps/backend và apps/web.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
