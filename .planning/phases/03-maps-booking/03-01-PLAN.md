---
phase: 03-maps-booking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/database/prisma/schema.prisma, packages/shared/src/ride.schema.ts, packages/shared/src/booking.schema.ts, apps/backend/src/routes/booking.routes.ts, apps/backend/src/controllers/booking.controller.ts, apps/backend/src/services/booking.service.ts, apps/backend/src/server.ts]
autonomous: true
requirements: [MAPS-01, BOOK-01]

must_haves:
  truths:
    - "Schema Prisma chứa các trường tọa độ cho Ride và model Booking"
    - "Shared schemas cho Booking được định nghĩa hoàn chỉnh bằng Zod"
    - "Backend có sẵn các endpoint rỗng cho Booking"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Database structure for Maps & Booking"
    - path: "packages/shared/src/booking.schema.ts"
      provides: "Validation for booking requests"
    - path: "apps/backend/src/routes/booking.routes.ts"
      provides: "Booking API entry points"
  key_links:
    - from: "apps/backend/src/server.ts"
      to: "apps/backend/src/routes/booking.routes.ts"
      via: "app.use('/api/bookings', ...)"
---

<objective>
Thiết lập nền tảng dữ liệu và cấu trúc API cho Giai đoạn 3. 
Mục tiêu: Cập nhật cơ sở dữ liệu để hỗ trợ tọa độ bản đồ và đặt chỗ, đồng thời chuẩn bị các skeleton files cho Backend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-maps-booking/03-RESEARCH.md
@packages/database/prisma/schema.prisma
@packages/shared/src/ride.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật Prisma Schema</name>
  <files>packages/database/prisma/schema.prisma</files>
  <action>
    - Thêm các trường tọa độ vào model `Ride`: `originLat`, `originLng`, `destinationLat`, `destinationLng` (kiểu Float, optional hoặc mặc định 0).
    - Kiểm tra lại model `Booking` (đã tồn tại) và đảm bảo các quan hệ (`Relation`) với User và Ride là chính xác.
    - Chạy `npx prisma generate` trong package `database`.
    - Chạy migration để cập nhật database (ví dụ: `npx prisma migrate dev --name add_ride_coordinates_and_booking`).
  </action>
  <verify>
    <automated>cd packages/database && npx prisma validate</automated>
  </verify>
  <done>Database schema đã được cập nhật và client đã được generate lại thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Shared Zod Schemas cho Booking & Ride</name>
  <files>packages/shared/src/booking.schema.ts, packages/shared/src/ride.schema.ts</files>
  <action>
    - Trong `packages/shared/src/ride.schema.ts`: Cập nhật `createRideSchema` để bao gồm 4 trường tọa độ mới (validate number).
    - Tạo `packages/shared/src/booking.schema.ts`:
      - `createBookingSchema`: validate `rideId` và `seats` (số ghế đặt).
      - `updateBookingStatusSchema`: validate trạng thái mới (CONFIRMED, REJECTED, CANCELLED).
    - Export các types tương ứng.
  </action>
  <verify>
    <automated>cd packages/shared && npm run build</automated>
  </verify>
  <done>Các schema validation đã sẵn sàng để sử dụng cho cả Backend và Frontend.</done>
</task>

<task type="auto">
  <name>Task 3: Thiết lập Backend Booking Skeleton</name>
  <files>apps/backend/src/controllers/booking.controller.ts, apps/backend/src/services/booking.service.ts, apps/backend/src/routes/booking.routes.ts, apps/backend/src/server.ts</files>
  <action>
    - Tạo `booking.service.ts` với các hàm skeleton: `createBooking`, `updateBookingStatus`, `cancelBooking`, `getUserBookings`, `getRideBookings`.
    - Tạo `booking.controller.ts` xử lý các request HTTP và gọi service.
    - Tạo `booking.routes.ts` định nghĩa các route:
      - `POST /` (Hành khách đặt chỗ)
      - `PATCH /:id/status` (Tài xế duyệt/từ chối)
      - `DELETE /:id` (Hành khách hủy)
      - `GET /my-bookings` (Danh sách của tôi)
    - Đăng ký route mới trong `apps/backend/src/server.ts` tại `/api/bookings`.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:5000/api/bookings -I</automated>
  </verify>
  <done>Cấu trúc API cho Booking đã được thiết lập và có thể truy cập (trả về 401 hoặc skeleton response).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Dữ liệu đầu vào từ người dùng (tọa độ, số ghế) phải được validate qua Zod |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Ride Coordinates | mitigate | Sử dụng Zod để đảm bảo tọa độ là số hợp lệ |
| T-03-01-02 | Information Disclosure | Booking Data | mitigate | Sử dụng Auth middleware để chỉ chủ sở hữu mới xem được booking của mình |
</threat_model>

<verification>
Chạy `npm run build` ở root để đảm bảo các gói shared được đồng bộ hóa thành công.
</verification>

<success_criteria>
- [ ] Lệnh `prisma validate` thành công.
- [ ] `packages/shared` build thành công với các schema mới.
- [ ] Server backend khởi chạy không lỗi và nhận diện route `/api/bookings`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-maps-booking/03-01-SUMMARY.md`
</output>
