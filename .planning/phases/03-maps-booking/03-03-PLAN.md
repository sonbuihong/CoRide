---
phase: 03-maps-booking
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [apps/web/src/lib/maps-loader.ts, apps/web/src/components/ui/address-autocomplete.tsx, apps/web/src/components/rides/ride-post-form.tsx, apps/web/src/app/rides/post/page.tsx]
autonomous: true
requirements: [MAPS-01]

must_haves:
  truths:
    - "Người dùng có thể nhập địa chỉ và nhận được gợi ý từ Google Maps"
    - "Trang đăng chuyến đi lưu được cả địa chỉ String và tọa độ Lat/Lng"
  artifacts:
    - path: "apps/web/src/components/ui/address-autocomplete.tsx"
      provides: "Reusable address input with Google Places API"
    - path: "apps/web/src/lib/maps-loader.ts"
      provides: "Singleton loader for Google Maps Script"
---

<objective>
Tích hợp Google Maps Platform vào ứng dụng Web và triển khai component Autocomplete địa chỉ.
Cập nhật quy trình đăng chuyến đi để thu thập tọa độ địa lý chính xác.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-maps-booking/03-RESEARCH.md
@apps/web/src/components/rides/ride-post-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cấu hình Google Maps Loader & Provider</name>
  <files>apps/web/src/lib/maps-loader.ts, apps/web/src/app/layout.tsx</files>
  <action>
    - Cài đặt thư viện `@react-google-maps/api` và `use-places-autocomplete` trong `apps/web`.
    - Tạo `maps-loader.ts` để quản lý việc tải Script Google Maps an toàn, sử dụng biến môi trường `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`.
    - Bọc `RootLayout` hoặc phần cần thiết bằng `LoadScript` hoặc cơ chế tương đương để sẵn sàng sử dụng các API Maps.
  </action>
  <verify>
    <automated>npm list @react-google-maps/api</automated>
  </verify>
  <done>Thư viện bản đồ đã được cài đặt và Script đã được load thành công vào Frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng component AddressAutocomplete</name>
  <files>apps/web/src/components/ui/address-autocomplete.tsx</files>
  <action>
    - Tạo component `AddressAutocomplete` sử dụng `use-places-autocomplete`.
    - Giao diện sử dụng Shadcn UI Input và Popover/Command để hiển thị danh sách gợi ý.
    - Component phải trả về cả `description` (địa chỉ chuỗi) và `coordinates` (lat, lng) khi người dùng chọn một địa điểm.
    - Hỗ trợ ngôn ngữ Tiếng Việt cho kết quả tìm kiếm.
  </action>
  <verify>
    <automated>grep -r "use-places-autocomplete" apps/web/src/components/ui/</automated>
  </verify>
  <done>Component Autocomplete hoạt động mượt mà và trả về dữ liệu địa lý chính xác.</done>
</task>

<task type="auto">
  <name>Task 3: Cập nhật Form Đăng chuyến đi (Ride Post)</name>
  <files>apps/web/src/components/rides/ride-post-form.tsx</files>
  <action>
    - Thay thế các ô nhập `origin` và `destination` truyền thống bằng component `AddressAutocomplete` mới.
    - Cập nhật state và schema validation ở Frontend để bao gồm tọa độ lat/lng.
    - Đảm bảo khi submit form, dữ liệu tọa độ được gửi lên Backend API cùng với thông tin chuyến đi.
    - Toàn bộ giao diện form phải là **Tiếng Việt 100%**.
  </action>
  <verify>
    <automated>grep -r "originLat" apps/web/src/components/rides/</automated>
  </verify>
  <done>Người dùng có thể đăng chuyến đi với địa chỉ được chọn từ Google Maps, dữ liệu tọa độ được lưu trữ thành công.</done>
</task>

</tasks>

<success_criteria>
- [ ] Autocomplete hiển thị gợi ý địa chỉ tại Việt Nam.
- [ ] Form đăng chuyến đi gửi thành công 4 trường tọa độ lên Backend.
- [ ] Không có từ tiếng Anh trong giao diện đăng chuyến.
</success_criteria>

<output>
After completion, create `.planning/phases/03-maps-booking/03-03-SUMMARY.md`
</output>
