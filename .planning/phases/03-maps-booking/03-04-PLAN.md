---
phase: 03-maps-booking
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: [apps/web/src/app/rides/[id]/page.tsx, apps/web/src/components/rides/ride-route-map.tsx, apps/web/src/components/booking/booking-button.tsx, apps/web/src/app/my-bookings/page.tsx, apps/web/src/components/booking/passenger-booking-list.tsx]
autonomous: true
requirements: [MAPS-01, BOOK-01]

must_haves:
  truths:
    - "Trang chi tiết chuyến đi hiển thị bản đồ với lộ trình (Route) giữa điểm đi và điểm đến"
    - "Hành khách có thể click nút 'Đặt chỗ' và yêu cầu được gửi tới Backend"
    - "Hành khách xem được danh sách các đặt chỗ của mình và trạng thái của chúng"
  artifacts:
    - path: "apps/web/src/components/rides/ride-route-map.tsx"
      provides: "Visual route display on Google Maps"
    - path: "apps/web/src/app/my-bookings/page.tsx"
      provides: "Passenger-centric booking overview"
---

<objective>
Xây dựng giao diện hiển thị lộ trình trực quan và luồng đặt chỗ cho hành khách.
Người dùng có thể quản lý các yêu cầu đi nhờ xe của mình thông qua giao diện Tiếng Việt 100%.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-maps-booking/03-02-SUMMARY.md
@.planning/phases/03-maps-booking/03-03-SUMMARY.md
@apps/web/src/app/rides/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hiển thị Bản đồ lộ trình trên trang Chi tiết Chuyến đi</name>
  <files>apps/web/src/components/rides/ride-route-map.tsx, apps/web/src/app/rides/[id]/page.tsx</files>
  <action>
    - Tạo component `RideRouteMap` nhận vào tọa độ `origin` và `destination`.
    - Sử dụng `DirectionsService` và `DirectionsRenderer` từ `@react-google-maps/api` để tính toán và hiển thị lộ trình trên bản đồ.
    - Nhúng component này vào trang chi tiết chuyến đi (`/rides/[id]`).
  </action>
  <verify>
    <automated>grep -r "DirectionsService" apps/web/src/components/rides/</automated>
  </verify>
  <done>Bản đồ hiển thị chính xác tuyến đường giữa hai điểm khi người dùng xem chi tiết chuyến đi.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Nút Đặt chỗ & Thông báo</name>
  <files>apps/web/src/components/booking/booking-button.tsx, apps/web/src/app/rides/[id]/page.tsx</files>
  <action>
    - Tạo component `BookingButton` với logic gọi API `POST /api/bookings`.
    - Xử lý các trạng thái: Đang gửi (loading), Thành công (success), Lỗi (error - ví dụ: hết ghế).
    - Sử dụng **Sonner Toast** để hiển thị thông báo bằng **Tiếng Việt 100%** (ví dụ: "Đặt chỗ thành công! Đang chờ tài xế duyệt").
    - Ẩn nút nếu `availableSeats == 0` hoặc người dùng là chủ chuyến đi.
  </action>
  <verify>
    <automated>grep -r "/api/bookings" apps/web/src/components/booking/</automated>
  </verify>
  <done>Hành khách có thể thực hiện đặt chỗ trực tiếp từ giao diện Web và nhận thông báo phản hồi rõ ràng.</done>
</task>

<task type="auto">
  <name>Task 3: Xây dựng trang Danh sách đặt chỗ của tôi</name>
  <files>apps/web/src/app/my-bookings/page.tsx, apps/web/src/components/booking/passenger-booking-list.tsx</files>
  <action>
    - Tạo trang `/my-bookings` hiển thị danh sách các booking mà người dùng hiện tại đã tạo.
    - Hiển thị đầy đủ thông tin: Tên chuyến đi, Thời gian, Trạng thái (Chờ duyệt, Đã xác nhận, Bị từ chối, Đã hủy).
    - Thêm nút "Hủy yêu cầu" (nếu trạng thái là PENDING hoặc CONFIRMED) gọi API `DELETE /api/bookings/:id`.
    - Giao diện sử dụng Shadcn Table hoặc Cards, ngôn ngữ **Tiếng Việt 100%**.
  </action>
  <verify>
    <automated>ls apps/web/src/app/my-bookings/page.tsx</automated>
  </verify>
  <done>Hành khách quản lý được toàn bộ lịch sử đặt chỗ của mình một cách thuận tiện.</done>
</task>

</tasks>

<success_criteria>
- [ ] Bản đồ hiển thị lộ trình (đường màu xanh) giữa hai điểm.
- [ ] Nút Đặt chỗ hoạt động và gửi dữ liệu chính xác lên Backend.
- [ ] Trang My Bookings hiển thị danh sách thực tế từ Database.
</success_criteria>

<output>
After completion, create `.planning/phases/03-maps-booking/03-04-SUMMARY.md`
</output>
