---
phase: 03-maps-booking
plan: 05
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [apps/web/src/app/booking-requests/page.tsx, apps/web/src/components/booking/driver-booking-request-list.tsx, apps/web/src/components/booking/booking-action-buttons.tsx]
autonomous: true
requirements: [BOOK-02]

must_haves:
  truths:
    - "Tài xế xem được danh sách các yêu cầu đặt chỗ cho các chuyến đi của mình"
    - "Tài xế có thể duyệt (Chấp nhận) hoặc từ chối yêu cầu bằng Tiếng Việt"
  artifacts:
    - path: "apps/web/src/app/booking-requests/page.tsx"
      provides: "Driver-centric booking request dashboard"
---

<objective>
Xây dựng giao diện cho tài xế quản lý các yêu cầu đặt chỗ từ hành khách.
Cho phép duyệt hoặc từ chối yêu cầu trực tiếp trên Web với trải nghiệm mượt mà.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-maps-booking/03-02-SUMMARY.md
@apps/web/src/app/my-rides/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng trang Yêu cầu đặt chỗ cho Tài xế</name>
  <files>apps/web/src/app/booking-requests/page.tsx, apps/web/src/components/booking/driver-booking-request-list.tsx</files>
  <action>
    - Tạo trang `/booking-requests` lấy dữ liệu tất cả các booking liên quan đến các chuyến đi của tài xế hiện tại.
    - Nhóm theo chuyến đi hoặc hiển thị danh sách phẳng với thông tin hành khách (tên, số ghế đặt, thời gian yêu cầu).
    - Hiển thị trạng thái hiện tại của từng yêu cầu.
    - Ngôn ngữ: **Tiếng Việt 100%**.
  </action>
  <verify>
    <automated>ls apps/web/src/app/booking-requests/page.tsx</automated>
  </verify>
  <done>Tài xế có một giao diện tập trung để kiểm soát mọi yêu cầu đi nhờ xe.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai các nút Phê duyệt & Từ chối</name>
  <files>apps/web/src/components/booking/booking-action-buttons.tsx</files>
  <action>
    - Tạo các nút "Chấp nhận" (màu xanh) và "Từ chối" (màu đỏ) gọi API `PATCH /api/bookings/:id/status`.
    - Thêm xác nhận (Dialog/Popover) trước khi từ chối để tránh thao tác nhầm.
    - Cập nhật UI ngay lập tức khi hành động thành công (loại bỏ khỏi danh sách pending hoặc thay đổi nhãn trạng thái).
    - Hiển thị Toast thông báo bằng Tiếng Việt: "Đã xác nhận chuyến đi" hoặc "Đã từ chối yêu cầu".
  </action>
  <verify>
    <automated>grep -r "/status" apps/web/src/components/booking/</automated>
  </verify>
  <done>Tài xế thực hiện quyền duyệt/từ chối đặt chỗ một cách nhanh chóng và chính xác.</done>
</task>

</tasks>

<success_criteria>
- [ ] Trang Yêu cầu đặt chỗ hiển thị đúng dữ liệu hành khách.
- [ ] Hành động Chấp nhận làm thay đổi trạng thái booking thành CONFIRMED ở DB.
- [ ] Toàn bộ UI tuân thủ chuẩn Tiếng Việt 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/03-maps-booking/03-05-SUMMARY.md`
</output>
