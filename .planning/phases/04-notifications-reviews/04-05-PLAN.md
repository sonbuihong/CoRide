---
phase: 04-notifications-reviews
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified: [
  "code/apps/web/src/components/profile/review-list.tsx",
  "code/apps/web/src/components/rides/review-dialog.tsx",
  "code/apps/web/src/app/profile/page.tsx",
  "code/apps/web/src/app/rides/[id]/page.tsx"
]
autonomous: true
requirements: [REV-04, UI-REV]
must_haves:
  truths:
    - "Dialog đánh giá xuất hiện sau khi chuyến đi kết thúc"
    - "Danh sách đánh giá được hiển thị trên Profile người dùng"
    - "Thông tin Rating trung bình được hiển thị cùng avatar người dùng"
  artifacts:
    - path: "code/apps/web/src/components/rides/review-dialog.tsx"
      provides: "Giao diện form đánh giá (Rating & Comment)"
    - path: "code/apps/web/src/components/profile/review-list.tsx"
      provides: "Danh sách hiển thị đánh giá nhận được"
---

<objective>
Hoàn thiện giao diện Đánh giá (Review UI) và hiển thị uy tín người dùng trên Profile.

Mục tiêu:
- Xây dựng `ReviewDialog` để người dùng dễ dàng đánh giá đối phương sau mỗi chuyến đi.
- Hiển thị danh sách các đánh giá nhận được trên trang hồ sơ cá nhân.
- Cập nhật Profile để hiển thị điểm Rating trung bình của người dùng.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@code/apps/web/src/app/profile/page.tsx
@code/apps/web/src/app/rides/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Xây dựng ReviewDialog & Form</name>
  <files>code/apps/web/src/components/rides/review-dialog.tsx, code/apps/web/src/app/rides/[id]/page.tsx</files>
  <action>
    - Tạo `ReviewDialog` sử dụng `Dialog` component từ UI lib.
    - Form bao gồm: 5 sao (Rating) và một trường nhập văn bản (Comment).
    - Tích hợp vào trang chi tiết chuyến đi (`/rides/[id]`).
    - Chỉ hiển thị nút "Đánh giá ngay" nếu:
      - Chuyến đi trạng thái `COMPLETED`.
      - Người dùng hiện tại có liên quan (Tài xế hoặc Hành khách đã được confirm).
      - Chưa có review nào từ người dùng này cho chuyến đi này.
    - Ngôn ngữ: Tiếng Việt 100%.
  </action>
  <verify>
    <automated>Click thử nút Đánh giá trên chuyến đi COMPLETED</automated>
  </verify>
  <done>Giao diện gửi đánh giá hoàn thiện</done>
</task>

<task type="auto">
  <name>Hiển thị đánh giá trên Profile</name>
  <files>code/apps/web/src/components/profile/review-list.tsx, code/apps/web/src/app/profile/page.tsx</files>
  <action>
    - Tạo `ReviewList` component nhận danh sách review và hiển thị dạng Card.
    - Hiển thị nội dung bình luận, số sao, và tên người đánh giá.
    - Cập nhật trang Profile (`app/profile/page.tsx`) để fetch reviews nhận được (`/api/users/me/reviews` - cần tạo endpoint này hoặc include trong `/users/me`).
    - Hiển thị Rating trung bình ngay dưới Avatar (e.g., "4.8 ⭐ (12 đánh giá)").
  </action>
  <verify>
    <automated>Kiểm tra trang Profile có hiển thị đánh giá và điểm rating không</automated>
  </verify>
  <done>Thông tin uy tín người dùng được hiển thị minh bạch trên Profile</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Luồng đánh giá và hiển thị uy tín</what-built>
  <how-to-verify>
    - Đóng vai tài xế kết thúc chuyến đi (status COMPLETED).
    - Đóng vai hành khách vào trang chi tiết chuyến đi đó.
    - Thấy nút "Đánh giá ngay" hiện lên.
    - Gửi đánh giá 5 sao kèm nội dung "Tài xế rất nhiệt tình".
    - Vào profile của tài xế để check Rating đã tăng chưa và xem review mới trong danh sách.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- [ ] Luồng đánh giá hoạt động trơn tru (End-to-End).
- [ ] UI thân thiện, Tiếng Việt 100%, responsive.
- [ ] Rating trung bình hiển thị chính xác trên Profile.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/04-notifications-reviews/04-05-SUMMARY.md`
</output>
