---
phase: 04-notifications-reviews
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [
  "code/apps/web/src/components/layout/header.tsx",
  "code/apps/web/src/components/layout/notification-center.tsx",
  "code/apps/web/src/app/layout.tsx",
  "code/apps/web/src/app/page.tsx"
]
autonomous: true
requirements: [UI-NOTIF]
must_haves:
  truths:
    - "Dropdown thông báo hiển thị trên Header của toàn bộ các trang"
    - "Thông báo mới xuất hiện ngay lập tức không cần tải lại trang (SSE)"
    - "Số lượng thông báo chưa đọc được hiển thị dưới dạng badge"
  artifacts:
    - path: "code/apps/web/src/components/layout/notification-center.tsx"
      provides: "Giao diện và logic lắng nghe SSE"
    - path: "code/apps/web/src/components/layout/header.tsx"
      provides: "Thành phần Header dùng chung"
---

<objective>
Xây dựng giao diện Trung tâm thông báo (Notification Center) và tích hợp SSE vào Frontend.

Mục tiêu:
- Tái cấu trúc Header thành component dùng chung để quản lý trạng thái đăng nhập và thông báo.
- Triển khai `NotificationCenter` sử dụng Dropdown Menu để hiển thị danh sách thông báo.
- Lắng nghe sự kiện từ API SSE để cập nhật thông báo theo thời gian thực (Tiếng Việt 100%).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@code/apps/web/src/app/page.tsx
@code/apps/backend/src/controllers/notification.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Tạo Header component dùng chung</name>
  <files>code/apps/web/src/components/layout/header.tsx, code/apps/web/src/app/layout.tsx</files>
  <action>
    - Trích xuất logic Header từ `page.tsx` vào `components/layout/header.tsx`.
    - Thêm kiểm tra trạng thái đăng nhập (sử dụng auth context/hooks nếu có, hoặc fetch user me).
    - Tích hợp Header vào `RootLayout` (`app/layout.tsx`) để hiển thị trên mọi trang.
  </action>
  <verify>
    <automated>Kiểm tra Header hiển thị trên trang chủ và trang profile</automated>
  </verify>
  <done>Header dùng chung được thiết lập thành công</done>
</task>

<task type="auto">
  <name>Xây dựng NotificationCenter & SSE Client</name>
  <files>code/apps/web/src/components/layout/notification-center.tsx</files>
  <action>
    - Sử dụng `DropdownMenu` và `Bell` icon từ lucide-react.
    - Sử dụng `EventSource` API để kết nối tới `/api/notifications/subscribe`.
    - Khi nhận được event `notification`, thêm vào danh sách local state và hiển thị badge.
    - Triển khai hàm `markAsRead` khi người dùng click vào thông báo.
    - Ngôn ngữ: Tiếng Việt 100% (e.g., "Bạn có yêu cầu đặt chỗ mới", "Chuyến đi đã hoàn thành").
  </action>
  <verify>
    <automated>Mở console và check kết nối SSE trong Network tab</automated>
  </verify>
  <done>NotificationCenter hoạt động thời gian thực trên giao diện Web</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Hệ thống thông báo thời gian thực trên giao diện</what-built>
  <how-to-verify>
    - Đăng nhập vào 2 trình duyệt khác nhau (1 tài xế, 1 hành khách).
    - Hành khách đặt chỗ.
    - Kiểm tra Header của tài xế có hiện badge thông báo đỏ ngay lập tức không.
    - Click vào dropdown để xem nội dung thông báo.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- [ ] Header thống nhất trên toàn bộ website.
- [ ] SSE connection ổn định và tự động reconnect nếu mất mạng.
- [ ] Badge thông báo hiển thị đúng số lượng chưa đọc.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/04-notifications-reviews/04-04-SUMMARY.md`
</output>
