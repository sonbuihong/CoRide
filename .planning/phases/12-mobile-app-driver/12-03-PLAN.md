---
phase: 12-mobile-app-driver
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified: [code/apps/driver-mobile/app/(tabs)/index.tsx, code/apps/driver-mobile/app/(tabs)/schedule.tsx, code/apps/driver-mobile/src/hooks/useRides.ts]
autonomous: true
requirements: [MOBILE-02]

must_haves:
  truths:
    - "Tài xế xem được danh sách các chuyến đi mình đã tạo"
    - "Tài xế thấy được thông tin người đặt chỗ (booking) cho các chuyến đi đó"
    - "Dữ liệu được cập nhật từ API thực tế"
  artifacts:
    - path: "code/apps/driver-mobile/app/(tabs)/schedule.tsx"
      provides: "Ride schedule screen"
    - path: "code/apps/driver-mobile/src/hooks/useRides.ts"
      provides: "Data fetching for rides and bookings"
  key_links:
    - from: "code/apps/driver-mobile/src/hooks/useRides.ts"
      to: "Backend /rides"
      via: "React Query"
      pattern: "api\\.get.*rides"
---

<objective>
Xây dựng giao diện Dashboard và Lịch trình chuyến đi cho tài xế.

Mục đích: Hiển thị danh sách công việc và lịch trình di chuyển của tài xế một cách trực quan trên điện thoại.
Đầu ra: Màn hình lịch trình hiển thị danh sách chuyến đi và thông tin hành khách liên quan.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-mobile-app-driver/12-02-PLAN.md
@code/apps/web/src/app/driver/rides/page.tsx (Tham khảo logic lịch trình web)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng giao diện Dashboard và Tab Bar</name>
  <files>code/apps/driver-mobile/app/(tabs)/_layout.tsx, code/apps/driver-mobile/app/(tabs)/index.tsx</files>
  <action>
    - Tạo cấu trúc thư mục `app/(tabs)`.
    - Thiết lập `_layout.tsx` cho tabs sử dụng `expo-router` với các icon từ `@expo/vector-icons`.
    - Các tab gồm: Dashboard (Home), Lịch trình (Schedule), Thông báo (Notifications), Cá nhân (Profile).
    - Màn hình Dashboard (`index.tsx`): Hiển thị lời chào tài xế và tóm tắt chuyến đi trong ngày (số lượng chuyến, tổng thu nhập dự kiến).
  </action>
  <verify>
    <automated>ls code/apps/driver-mobile/app/(tabs)/_layout.tsx</automated>
  </verify>
  <done>Hệ thống Tab Bar hoạt động, chuyển đổi được giữa các màn hình chính.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai màn hình Lịch trình (Schedule) và Fetch dữ liệu</name>
  <files>code/apps/driver-mobile/app/(tabs)/schedule.tsx, code/apps/driver-mobile/src/hooks/useRides.ts, code/apps/driver-mobile/src/components/RideCard.tsx</files>
  <action>
    - Tạo hook `useRides.ts` sử dụng `useQuery` để fetch:
      - `GET /rides?driverId=...`: Lấy danh sách chuyến đi của tài xế hiện tại.
      - `GET /bookings/driver`: Lấy danh sách yêu cầu đặt chỗ gửi tới tài xế.
    - Xây dựng component `RideCard.tsx` hiển thị: Điểm đi/đến, Thời gian, Trạng thái, và danh sách Hành khách đã đặt.
    - Màn hình `schedule.tsx`: Hiển thị danh sách các `RideCard`, hỗ trợ Pull-to-refresh.
  </action>
  <verify>
    <automated>grep "useQuery" code/apps/driver-mobile/src/hooks/useRides.ts</automated>
  </verify>
  <done>Lịch trình chuyến đi hiển thị dữ liệu thực tế từ backend (Success Criteria #2).</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | Ride Data | mitigate | Backend phải filter dữ liệu theo `driverId` lấy từ JWT token, không tin tưởng param truyền từ client. |
</threat_model>

<verification>
Kiểm tra tab Schedule có hiện danh sách chuyến đi hay không.
Kiểm tra thông tin hành khách trong từng chuyến đi có đúng không.
</verification>

<success_criteria>
1. Hiển thị lịch trình chuyến đi thành công trên Mobile (Success Criteria #2).
2. Dữ liệu được fetch tự động và có thể làm mới bằng cử chỉ vuốt xuống.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/12-mobile-app-driver/12-03-SUMMARY.md`
</output>
