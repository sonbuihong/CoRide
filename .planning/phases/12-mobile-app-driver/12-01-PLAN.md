---
phase: 12-mobile-app-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/apps/driver-mobile/package.json, code/apps/driver-mobile/app/_layout.tsx, code/apps/driver-mobile/src/services/api.ts, code/apps/driver-mobile/src/store/useAuthStore.ts]
autonomous: true
requirements: [MOBILE-01]

must_haves:
  truths:
    - "Dự án Expo cho Tài xế được khởi tạo thành công trong monorepo"
    - "Cấu trúc thư mục tuân thủ Expo Router và Nativewind"
    - "Hệ thống xác thực (Auth infrastructure) sẵn sàng với SecureStore"
  artifacts:
    - path: "code/apps/driver-mobile/package.json"
      provides: "Expo project configuration"
    - path: "code/apps/driver-mobile/src/services/api.ts"
      provides: "Axios client with JWT interceptor"
    - path: "code/apps/driver-mobile/src/store/useAuthStore.ts"
      provides: "Global auth state management (Zustand)"
  key_links:
    - from: "code/apps/driver-mobile/src/services/api.ts"
      to: "Backend API"
      via: "Axios"
      pattern: "baseURL:.*process\\.env\\.EXPO_PUBLIC_API_URL"
---

<objective>
Khởi tạo dự án Expo cho ứng dụng Tài xế (@repo/driver-mobile) và thiết lập cơ sở hạ tầng cơ bản (Auth, API Client).

Mục đích: Tạo nền tảng vững chắc để phát triển các tính năng dành cho tài xế, tích hợp tốt với hệ thống monorepo hiện có.
Đầu ra: Project Expo hoạt động, có thể chạy lệnh start và có sẵn các dịch vụ lõi (Auth, API).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-mobile-app-driver/12-RESEARCH.md
@code/apps/mobile/package.json (Tham khảo cấu hình Expo)
@code/apps/backend/src/modules/auth/auth.controller.ts (Tham khảo API Login)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Khởi tạo project Driver Mobile trong Monorepo</name>
  <files>code/apps/driver-mobile/package.json, code/apps/driver-mobile/app/_layout.tsx, code/apps/driver-mobile/tailwind.config.js</files>
  <action>
    - Tạo thư mục `code/apps/driver-mobile`.
    - Sao chép cấu hình từ `code/apps/mobile` (package.json, tsconfig.json, app.json) và sửa đổi tên thành `@repo/driver-mobile`.
    - Cài đặt các dependencies cần thiết: `expo`, `expo-router`, `nativewind`, `tailwindcss`, `expo-secure-store`, `zustand`, `axios`, `@tanstack/react-query`.
    - Cấu hình Nativewind v4 và Tailwind để sử dụng trong project.
    - Tạo file `app/_layout.tsx` cơ bản với Stack Provider.
  </action>
  <verify>
    <automated>cd code/apps/driver-mobile && npx expo lint --no-fix</automated>
  </verify>
  <done>Project Expo khởi tạo không lỗi, nhận diện được workspace @repo/shared.</done>
</task>

<task type="auto">
  <name>Task 2: Thiết lập Auth Service và Store</name>
  <files>code/apps/driver-mobile/src/services/api.ts, code/apps/driver-mobile/src/store/useAuthStore.ts, code/apps/driver-mobile/.env</files>
  <action>
    - Tạo `src/store/useAuthStore.ts` sử dụng Zustand để quản lý `user`, `token`, và `isAuthenticated`.
    - Tích hợp `expo-secure-store` để lưu/tải token khi app khởi động.
    - Tạo `src/services/api.ts` cấu hình Axios instance:
      - `baseURL`: Lấy từ `process.env.EXPO_PUBLIC_API_URL`.
      - Request Interceptor: Tự động gắn Bearer Token từ SecureStore vào header.
      - Response Interceptor: Xử lý lỗi 401 (logout nếu token hết hạn).
    - Tạo file `.env` với `EXPO_PUBLIC_API_URL=http://10.0.2.2:5000` (cho Android emulator).
  </action>
  <verify>
    <automated>ls code/apps/driver-mobile/src/services/api.ts && ls code/apps/driver-mobile/src/store/useAuthStore.ts</automated>
  </verify>
  <done>Auth store và API client sẵn sàng sử dụng cho các task tiếp theo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → API | Untrusted network, require HTTPS and JWT |
| App → Storage | SecureStore provides encryption on device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Auth Token | mitigate | Sử dụng expo-secure-store thay vì AsyncStorage |
| T-12-02 | Spoofing | API Requests | mitigate | Sử dụng JWT Bearer token và validate tại backend |
</threat_model>

<verification>
Chạy `npm install` tại root monorepo để cập nhật symlinks.
Kiểm tra `code/apps/driver-mobile` có thể chạy `npx expo start`.
</verification>

<success_criteria>
1. Thư mục `apps/driver-mobile` tồn tại với đầy đủ file cấu hình.
2. File `api.ts` và `useAuthStore.ts` được viết đúng logic lưu trữ và interceptor.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/12-mobile-app-driver/12-01-SUMMARY.md`
</output>
