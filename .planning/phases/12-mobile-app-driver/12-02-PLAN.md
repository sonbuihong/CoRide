---
phase: 12-mobile-app-driver
plan: 02
type: execute
wave: 1
depends_on: [12-01]
files_modified: [code/apps/driver-mobile/app/(auth)/login.tsx, code/apps/driver-mobile/src/hooks/useAuth.ts, code/apps/driver-mobile/app/index.tsx]
autonomous: false
requirements: [MOBILE-02]

must_haves:
  truths:
    - "Người dùng có thể nhập email/mật khẩu và đăng nhập"
    - "Hệ thống chuyển hướng đúng sau khi đăng nhập thành công"
    - "Lỗi đăng nhập được hiển thị rõ ràng cho người dùng"
  artifacts:
    - path: "code/apps/driver-mobile/app/(auth)/login.tsx"
      provides: "Login screen UI"
    - path: "code/apps/driver-mobile/src/hooks/useAuth.ts"
      provides: "Authentication logic wrapper"
  key_links:
    - from: "code/apps/driver-mobile/app/(auth)/login.tsx"
      to: "Backend /auth/login"
      via: "useAuth hook"
      pattern: "api\\.post.*auth/login"
---

<objective>
Triển khai giao diện và logic đăng nhập cho ứng dụng Tài xế.

Mục đích: Cho phép tài xế truy cập vào hệ thống bằng tài khoản đã đăng ký.
Đầu ra: Màn hình đăng nhập hoạt động, lưu được session và chuyển hướng vào Dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-mobile-app-driver/12-01-PLAN.md
@code/apps/web/src/app/(auth)/login/page.tsx (Tham khảo giao diện login web)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng giao diện Đăng nhập</name>
  <files>code/apps/driver-mobile/app/(auth)/login.tsx, code/apps/driver-mobile/src/components/ui/Input.tsx</files>
  <action>
    - Tạo thư mục `app/(auth)`.
    - Tạo màn hình `login.tsx` với:
      - Form nhập Email và Password sử dụng `react-hook-form` và `zod` (để validate).
      - Styling bằng Nativewind (Tailwind).
      - Nút "Đăng nhập" có trạng thái loading.
      - Xử lý hiển thị lỗi khi API trả về lỗi (sai thông tin, server error).
    - Tạo các thành phần UI nhỏ như `Input.tsx`, `Button.tsx` nếu cần thiết để tái sử dụng.
  </action>
  <verify>
    <automated>ls code/apps/driver-mobile/app/(auth)/login.tsx</automated>
  </verify>
  <done>Giao diện login hiển thị đúng layout và validate input thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Logic Đăng nhập và Redirect</name>
  <files>code/apps/driver-mobile/src/hooks/useAuth.ts, code/apps/driver-mobile/app/index.tsx</files>
  <action>
    - Tạo hook `useAuth.ts` sử dụng `react-query`'s `useMutation` để gọi API `/auth/login`.
    - Sau khi thành công:
      - Cập nhật `useAuthStore` với dữ liệu user và token.
      - Lưu token vào `SecureStore`.
      - Sử dụng `router.replace('/(tabs)')` để chuyển hướng.
    - Cập nhật `app/index.tsx` để kiểm tra trạng thái đăng nhập khi khởi động app:
      - Nếu đã có token -> chuyển sang `/(tabs)`.
      - Nếu chưa có -> chuyển sang `/(auth)/login`.
  </action>
  <verify>
    <automated>grep "useMutation" code/apps/driver-mobile/src/hooks/useAuth.ts</automated>
  </verify>
  <done>Logic đăng nhập hoạt động, app tự động nhận diện session cũ.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Luồng Đăng nhập hoàn chỉnh trên Mobile</what-built>
  <how-to-verify>
    1. Chạy app trên Android Emulator: `npm run android` trong `apps/driver-mobile`.
    2. Nhập thông tin tài khoản tài xế hợp lệ.
    3. Kiểm tra xem có đăng nhập thành công và chuyển vào màn hình chính không.
    4. Tắt app và mở lại xem có giữ được phiên đăng nhập không.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Elevation of Privilege | Login | mitigate | Kiểm tra role 'DRIVER' của user sau khi đăng nhập thành công. |
</threat_model>

<verification>
Thử nghiệm đăng nhập với tài khoản không tồn tại -> hiện lỗi.
Thử nghiệm đăng nhập với tài khoản tài xế -> vào dashboard.
</verification>

<success_criteria>
1. Đăng nhập thành công trên Emulator (Success Criteria #1).
2. Token được lưu trữ bảo mật trong SecureStore.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/12-mobile-app-driver/12-02-SUMMARY.md`
</output>
