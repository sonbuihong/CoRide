---
phase: 12-mobile-app-driver
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified: [code/apps/driver-mobile/src/services/socket.ts, code/apps/driver-mobile/app/(tabs)/notifications.tsx, code/apps/driver-mobile/src/hooks/useNotifications.ts]
autonomous: true
requirements: [MOBILE-02]

must_haves:
  truths:
    - "App nhận được thông báo thời gian thực khi có booking mới"
    - "Thông báo hiển thị trên khay hệ thống (System Notification) khi app ở background"
    - "Danh sách thông báo lịch sử được lưu trữ và hiển thị"
  artifacts:
    - path: "code/apps/driver-mobile/src/services/socket.ts"
      provides: "Socket.IO client connection"
    - path: "code/apps/driver-mobile/src/hooks/useNotifications.ts"
      provides: "Notification management logic"
  key_links:
    - from: "code/apps/driver-mobile/src/services/socket.ts"
      to: "Backend Socket Server"
      via: "Websocket"
      pattern: "io.*process\\.env\\.EXPO_PUBLIC_SOCKET_URL"
---

<objective>
Tích hợp hệ thống thông báo thời gian thực (Realtime Notifications) cho ứng dụng Tài xế.

Mục đích: Đảm bảo tài xế nhận được thông tin đặt chuyến ngay lập tức để có thể xử lý kịp thời.
Đầu ra: Thông báo đẩy (Push) và thông báo trong app (In-app) hoạt động ổn định.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-mobile-app-driver/12-RESEARCH.md
@code/apps/web/src/hooks/useSocket.ts (Tham khảo logic socket web)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thiết lập Socket.IO Client và In-app Notification</name>
  <files>code/apps/driver-mobile/src/services/socket.ts, code/apps/driver-mobile/src/hooks/useNotifications.ts, code/apps/driver-mobile/app/(tabs)/notifications.tsx</files>
  <action>
    - Tạo `src/services/socket.ts` để quản lý kết nối Socket.IO:
      - Tự động kết nối khi user đăng nhập.
      - Sử dụng token để authenticate socket.
      - Lắng nghe sự kiện `booking:new`.
    - Tạo hook `useNotifications.ts` để:
      - Quản lý danh sách thông báo cục bộ.
      - Hiển thị Toast (sử dụng thư viện Toast hoặc Custom UI) khi nhận event socket.
    - Xây dựng màn hình `notifications.tsx` để hiển thị lịch sử thông báo.
  </action>
  <verify>
    <automated>grep "socket\.on" code/apps/driver-mobile/src/services/socket.ts</automated>
  </verify>
  <done>Thông báo thời gian thực hiển thị trong app khi có sự kiện mới.</done>
</task>

<task type="auto">
  <name>Task 2: Tích hợp expo-notifications cho System Push</name>
  <files>code/apps/driver-mobile/src/services/notificationService.ts, code/apps/driver-mobile/app/_layout.tsx</files>
  <action>
    - Cấu hình `expo-notifications` trong `_layout.tsx`:
      - Xin quyền thông báo (Permissions).
      - Thiết lập Notification Handler để hiển thị thông báo ngay cả khi app đang mở (Foreground).
    - Tạo `src/services/notificationService.ts` để trigger thông báo hệ thống khi socket nhận event:
      - Sử dụng `Notifications.scheduleNotificationAsync` để hiện thông báo cục bộ ngay lập tức.
      - Đảm bảo âm thanh và rung hoạt động trên Android.
  </action>
  <verify>
    <automated>grep "Notifications\.scheduleNotificationAsync" code/apps/driver-mobile/src/services/notificationService.ts</automated>
  </verify>
  <done>Thông báo hệ thống xuất hiện trên khay thông báo khi có booking mới (Success Criteria #3).</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Denial of Service | Socket Connection | mitigate | Sử dụng cơ chế reconnection có giới hạn và timeout hợp lý. |
</threat_model>

<verification>
Sử dụng một trình duyệt khác đăng nhập vai Passenger và thực hiện Đặt chuyến.
Kiểm tra xem điện thoại chạy Driver App có nhận được thông báo không.
</verification>

<success_criteria>
1. Nhận được thông báo thời gian thực trên Mobile (Success Criteria #3).
2. Thông báo hiển thị đúng cả khi app ở foreground và background.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/12-mobile-app-driver/12-04-SUMMARY.md`
</output>
