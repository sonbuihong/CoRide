---
phase: "06"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: [
  "apps/web/package.json",
  "apps/web/playwright.config.ts",
  "apps/web/tests/e2e/auth.spec.ts",
  "apps/web/tests/e2e/ride.spec.ts"
]
autonomous: true
requirements: ["E2E-01", "E2E-02"]

must_haves:
  truths:
    - "Playwright được cài đặt trong apps/web"
    - "Test case cho Đăng nhập/Đăng ký hoàn thành"
    - "Test case cho Đăng chuyến và Tìm kiếm chuyến đi hoàn thành"
---

<objective>
Triển khai kiểm thử E2E cho ứng dụng Web sử dụng Playwright để đảm bảo các luồng nghiệp vụ chính hoạt động ổn định.
</objective>

<tasks>
<task type="auto">
  <name>Cài đặt Playwright trong apps/web</name>
  <action>
    - Chạy `npm install -D @playwright/test` trong `apps/web`.
    - Khởi tạo `playwright.config.ts`.
  </action>
</task>

<task type="auto">
  <name>Viết test case cho luồng Authentication</name>
  <action>
    - Tạo `tests/e2e/auth.spec.ts`.
    - Viết test cho: Đăng ký thành công, Đăng nhập thành công, Đăng nhập lỗi.
  </action>
</task>

<task type="auto">
  <name>Viết test case cho luồng Ride Management</name>
  <action>
    - Tạo `tests/e2e/ride.spec.ts`.
    - Viết test cho: Đăng chuyến đi (Driver), Tìm kiếm chuyến đi (Passenger).
  </action>
</task>
</tasks>

<verification>
Chạy `npx playwright test` và đảm bảo các bài test đều pass.
</verification>
