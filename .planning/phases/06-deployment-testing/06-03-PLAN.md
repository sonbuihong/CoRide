---
phase: "06"
plan: "03"
type: "execute"
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: [
  "apps/backend/Dockerfile",
  "apps/web/Dockerfile",
  "docker-compose.yml",
  ".github/workflows/ci.yml"
]
autonomous: true
requirements: ["DEPLOY-01", "CI-01"]

must_haves:
  truths:
    - "Dockerfile cho Backend và Web hoàn thành"
    - "Docker Compose cấu hình đúng để chạy dự án cục bộ"
    - "GitHub Actions workflow cho CI hoàn thành"
---

<objective>
Docker hóa ứng dụng và thiết lập quy trình CI (Continuous Integration) cơ bản thông qua GitHub Actions.
</objective>

<tasks>
<task type="auto">
  <name>Dockerization (Backend & Web)</name>
  <action>
    - Tạo `apps/backend/Dockerfile`.
    - Tạo `apps/web/Dockerfile`.
    - Cập nhật `docker-compose.yml` ở thư mục gốc.
  </action>
</task>

<task type="auto">
  <name>Thiết lập GitHub Actions CI</name>
  <action>
    - Tạo `.github/workflows/ci.yml`.
    - Cấu hình: Run on Push/PR to main, install dependencies, lint, test.
  </action>
</task>
</tasks>

<verification>
Chạy `docker compose up -d --build` để verify việc build container thành công.
</verification>
