---
phase: "06"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified: [
  "apps/backend/src/server.ts",
  "apps/backend/src/controllers/ride.controller.ts",
  "packages/database/prisma/schema.prisma"
]
autonomous: true
requirements: ["SEC-01", "SEC-02", "PERF-01"]

must_haves:
  truths:
    - "Express-rate-limit được tích hợp trong Backend"
    - "Helmet được tích hợp trong Backend"
    - "Indexes cho bảng Ride đã được định nghĩa trong Prisma schema"
---

<objective>
Tăng cường bảo mật bằng Rate Limiting và Helmet, tối ưu hóa hiệu năng truy vấn chuyến đi thông qua Database Indexing.
</objective>

<tasks>
<task type="auto">
  <name>Bảo mật Backend (Security Hardening)</name>
  <action>
    - Cài đặt `express-rate-limit` và `helmet` trong `apps/backend`.
    - Cấu hình Middleware trong `src/server.ts`.
  </action>
</task>

<task type="auto">
  <name>Tối ưu hóa Database Indexing</name>
  <action>
    - Cập nhật `schema.prisma` tại `packages/database`.
    - Thêm `@@index([origin, destination, departureTime])` vào model `Ride`.
    - Chạy `npx prisma migrate dev` để áp dụng index.
  </action>
</task>
</tasks>

<verification>
Kiểm tra phản hồi của API và cấu trúc bảng trong Database (Prisma).
</verification>
